---
title: 未命名
date: '2026-06-04 18:07:00'
permalink: /z3jbrifl.html
---


在二叉树领域中，有一些特殊类型的二叉树被认为比较重要，它们在算法和数据结构中有着广泛的应用和影响。以下是几个比较重要的二叉树类型：

+ 二叉搜索树（BST）：在BST中，每个节点的左子树节点的值都小于该节点的值，右子树节点的值都大于该节点的值。BST的重要性在于它可以提供高效的查找、插入和删除操作，时间复杂度为 O(log n)（平均情况下）。
+ 平衡二叉树（AVL 树、红黑树等）：平衡二叉树是一种特殊的二叉搜索树，它保持树的左右子树高度差不超过一个固定的值（如1或2），从而保证了树的高度始终保持在对数级别，提供了更稳定的性能保证。
+ 堆（Heap）：堆是一种特殊的二叉树结构，分为最大堆和最小堆。最大堆中，每个节点的值都大于或等于其子节点的值；最小堆则相反。堆常被用于实现优先队列等数据结构，具有高效的插入和删除操作。

相关算法

+ 二叉树遍历：包括前序遍历、中序遍历、后序遍历和层序遍历等。这些遍历方法在树的搜索、输出、计算高度等方面都有着重要的作用，是二叉树操作中的基础。
+ 二叉搜索树的平衡化方法：AVL 树、红黑树等平衡化方法是解决二叉搜索树退化的重要手段，保证了树的高度始终保持在对数级别，提高了搜索、插入和删除等操作的效率。

这些二叉树类型和相关的算法在计算机科学中具有广泛的应用，并且在各种数据结构和算法问题中经常被用到。

### 二叉搜索树 BST
二叉排序树，也称为二叉搜索树（Binary Search Tree，BST），是一种二叉树的特殊形式。

[https://www.cs.usfca.edu/~galles/visualization/BST.html](https://www.cs.usfca.edu/~galles/visualization/BST.html)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713262939313-33ccc88d-4e48-47b4-8c59-ebc9662b6ac3.png)

它具有以下性质：

+ 有序性质：对于树中的每个节点，其左子树中的所有节点的值都小于该节点的值，而右子树中的所有节点的值都大于该节点的值。这个性质保证了树的有序性，使得在BST中可以进行高效的搜索操作。
+ 唯一性质：BST中不允许存在相同值的节点，每个节点的值必须是唯一的。
+ 二叉树结构：每个节点最多有两个子节点，分别称为左子节点和右子节点。这个结构使得BST的每个节点都可以通过比较操作来确定其在树中的位置，从而实现高效的搜索、插入和删除操作。

由于其有序性质和二叉树结构，二叉排序树通常被用作实现动态集合（如集合、映射等）的数据结构。

其具有以下优点：

高效的搜索：由于有序性质，可以利用二叉搜索树的特性进行快速搜索操作。在平均情况下，搜索的时间复杂度为O(log n)，其中n是树中节点的数量。

方便的插入和删除：插入和删除操作都可以在O(log n)的时间内完成，因为在BST中插入和删除节点时，只需要通过比较操作确定节点的位置，并进行相应的调整。

易于实现：二叉排序树的基本操作（搜索、插入、删除）相对简单，易于实现和理解。



然而，BST的性能在最坏情况下可能退化为O(n)，例如当树退化为链表时。为了解决这个问题，可以采用平衡二叉搜索树（AVL树）、红黑树等，这些树能够保持树的平衡，从而保证了操作的平均性能。



:::tips
二叉搜索树具有下列性质的二叉树：

若左子树不空，则左子树上所有节点的值均小于它的根节点的值；

若右子树不空，则右子树上所有节点的值均大于它的根节点的值；

左、右子树也分别为二叉排序树；

没有键值相等的节点。

:::

对于一个二叉查找树，常规操作有插入，查找，删除，找父节点，求最大值，求最小值。

二叉搜索树的中序遍历的结果是一个有序数组



#### 验证二叉搜索树
[https://leetcode.cn/problems/validate-binary-search-tree/description/](https://leetcode.cn/problems/validate-binary-search-tree/description/)

给你一个二叉树的根节点 root ，判断其是否是一个有效的二叉搜索树。

有效 二叉搜索树定义如下：

+ 节点的左子树只包含 小于 当前节点的数。
+ 节点的右子树只包含 大于 当前节点的数。
+ 所有左子树和右子树自身必须也是二叉搜索树。

```java
public boolean isValidBST(TreeNode root) {
    Deque<TreeNode> stack = new LinkedList<TreeNode>();
    double inorder = -Double.MAX_VALUE;

    while (!stack.isEmpty() || root != null) {
        while (root != null) {
            stack.push(root);
            root = root.left;
        }
        root = stack.pop();
          // 如果中序遍历得到的节点的值小于等于前一个 inorder，说明不是二叉搜索树
        if (root.val <= inorder) {
            return false;
        }
        inorder = root.val;
        root = root.right;
    }
    return true;
}
```



使用递归

```java
public boolean isValidBST(TreeNode root) {
    return isValidBST(root, Long.MIN_VALUE, Long.MAX_VALUE);
}

private boolean isValidBST(TreeNode node, long min, long max) {
    if (node == null) {
        return true;
    }
    // 当前节点值必须在合法范围内
    if (node.val <= min || node.val >= max) {
        return false;
    }
    // 递归检查左子树和右子树
    return isValidBST(node.left, min, node.val) &&
            isValidBST(node.right, node.val, max);
}
```



C++版

```cpp
#include <climits>
using namespace std;

struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;

    TreeNode() : val(0), left(nullptr), right(nullptr){}
    TreeNode(int x) : val(x), left(nullptr), right(nullptr){}
    TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right){}
};

class Solution {
public:
    // 当确定了当前节点的值 也就划分出左右子树的值的范围
    // 左子树 [min, root->val]
    // 右子树 [root->val, max]
    bool isValidBST(TreeNode* root) {
        return isValidBSTByRec(root, LONG_MIN, LONG_MAX);
    }

    bool isValidBSTByRec(TreeNode * root, long min, long max)
    {
        // 空节点是满足的
        if (root == nullptr) return true;
        // 一旦发现不满足 返回false
        if (root -> val <= min || root -> val >= max)
        {
            return false;
        }

        // 当左右子树都满足时 才返回true
        bool left = isValidBSTByRec(root -> left, min, root -> val);
        bool right = isValidBSTByRec(root -> right, root -> val, max);
        return left && right;
    }
};
```



#### 基本操作
##### 1、搜索操作
如果要在树中 搜索是否有 值为4的节点

先从根节点遍历 4 < 5 所以如果存在，会出现在根节点的左子树中；  
遍历根节点的左孩子 4 > 2 所以如果存在，会出现在左孩子的右子树中；  
遍历2节点的右孩子 4 = 4 找到此节点



###### 二叉搜索树中的搜索
[700. 二叉搜索树中的搜索 - 力扣（LeetCode）](https://leetcode.cn/problems/search-in-a-binary-search-tree/description/)

```java
public TreeNode searchBST(TreeNode root, int val) {
    // 基本情况：树为空或当前节点值等于目标值
    if (root == null || root.val == val) {
        return root;
    }

    // 如果目标值小于当前节点的值，则在左子树中查找
    if (val < root.val) {
        return searchBST(root.left, val);
    }
    // 如果目标值大于当前节点的值，则在右子树中查找
    else {
        return searchBST(root.right, val);
    }
}
```



###### 寻找二叉搜索树中的目标节点
[<font style="color:#117CEE;">https://leetcode.cn/problems/er-cha-sou-suo-shu-de-di-kda-jie-dian-lcof/description/](https://leetcode.cn/problems/er-cha-sou-suo-shu-de-di-kda-jie-dian-lcof/description/)

```java
public int findTargetNode(TreeNode root, int cnt) {
    // 中序遍历的结果
    List<Integer> list = new ArrayList<>();
    Stack<TreeNode> stack = new Stack<>();
    TreeNode curr = root;
    while (curr != null || !stack.isEmpty()) {
        // 入栈
        // 1 2 4 8 null
        while (curr != null) {
            stack.push(curr);
            curr = curr.left;
        }
        // 出栈
        curr = stack.pop();
        list.add(curr.val);
        curr = curr.right;
    }
    return list.get(list.size() - cnt);
}
```

###### 
##### 2、插入操作
插入之后仍然要保持是一棵BST 二叉查找树  
  
插入节点时  
1） 判断是否存在同样的结点，如果结点已存在，不做操作  
2） 如果结点不存在，作为一个新的叶子结点插入到合适的位置中

插入节点8  
1） 依次遍历 5 6 7 结点，发现没有要插入的结点存在，可以进行插入操作  
2） 将结点8 作为7结点的右子树 插入进去

 ![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310430113-c07ade8a-80bf-4e80-b4d7-4ff25ec2e598.png)



###### 二叉搜索树中的插入操作
[701. 二叉搜索树中的插入操作 - 力扣（LeetCode）](https://leetcode.cn/problems/insert-into-a-binary-search-tree/description/)

给定二叉搜索树（BST）的根节点 root 和要插入树中的值 value ，将值插入二叉搜索树。 返回插入后二叉搜索树的根节点。 输入数据 保证 ，新值和原始二叉搜索树中的任意节点值都不同。

注意，可能存在多种有效的插入方式，只要树在插入后仍保持为二叉搜索树即可。 你可以返回 任意有效的结果 。

```java
public TreeNode insertIntoBST(TreeNode root, int val) {
    // 如果树为空，创建一个新节点作为根节点
    if (root == null) {
        return new TreeNode(val);
    }

    // 使用辅助方法插入新值
    insertHelper(root, val);
    return root;
}

private void insertHelper(TreeNode node, int value) {
    // 找到插入位置
    if (value < node.val) {
        // 插入到左子树
        if (node.left == null) {
            node.left = new TreeNode(value);
        } else {
            insertHelper(node.left, value);
        }
    } else {
        // 插入到右子树
        if (node.right == null) {
            node.right = new TreeNode(value);
        } else {
            insertHelper(node.right, value);
        }
    }
}
```



###### 恢复二叉搜索树
[https://leetcode.cn/problems/recover-binary-search-tree/description/](https://leetcode.cn/problems/recover-binary-search-tree/description/)

```java
public void recoverTree(TreeNode root) {
    List<Integer> nums = new ArrayList<Integer>();
    inorder(root, nums);
    int[] swapped = findTwoSwapped(nums);
    recover(root, 2, swapped[0], swapped[1]);
}

public void inorder(TreeNode root, List<Integer> nums) {
    if (root == null) {
        return;
    }
    inorder(root.left, nums);
    nums.add(root.val);
    inorder(root.right, nums);
}

public int[] findTwoSwapped(List<Integer> nums) {
    int n = nums.size();
    int index1 = -1, index2 = -1;
    for (int i = 0; i < n - 1; ++i) {
        if (nums.get(i + 1) < nums.get(i)) {
            index2 = i + 1;
            if (index1 == -1) {
                index1 = i;
            } else {
                break;
            }
        }
    }
    int x = nums.get(index1), y = nums.get(index2);
    return new int[] { x, y };
}

public void recover(TreeNode root, int count, int x, int y) {
    if (root != null) {
        if (root.val == x || root.val == y) {
            root.val = root.val == x ? y : x;
            if (--count == 0) {
                return;
            }
        }
        recover(root.right, count, x, y);
        recover(root.left, count, x, y);
    }
}
```

##### 3、删除操作
1） 当要删除的节点是叶子节点时，直接从父节点上移除  
![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310439227-fc46025a-5186-4c48-acb9-901130986d33.png)

2） 当要删除的节点只有一个子节点时，将子节点链接到当前节点的父节点上。

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310443357-b5df3a29-9720-4503-9fa0-aee863d09e89.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310447103-cb0b206b-3d36-4f15-b0a1-acc921f73745.png)

3）当要删除的结点有两个结点时，从删除结点的右子树中找到最小值的结点，移到删除结点的位置中，并且原最小值的结点删掉

 ![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310452500-e3fe2430-c3e0-4c17-b1cb-cd36d8ad13f6.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310456843-61b4a600-fd5d-44ca-a4ca-46c5090124ee.png)

说明： 将2的右子树中最小值3 移动到2所在的位置 并且3节点移除



###### 修剪二叉搜索树
[<font style="color:#117CEE;">https://leetcode.cn/problems/trim-a-binary-search-tree/description/](https://leetcode.cn/problems/trim-a-binary-search-tree/description/)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713264667705-a652084b-284f-42e1-a2f6-b928a3f0ec1f.png)

```java
public TreeNode trimBST(TreeNode root, int low, int high) {
    if (root == null) {
        return null;
    }
    if (root.val < low) {
        return trimBST(root.right, low, high);
    } else if (root.val > high) {
        return trimBST(root.left, low, high);
    } else {
        root.left = trimBST(root.left, low, high);
        root.right = trimBST(root.right, low, high);
        return root;
    }
}
```



###### 删除二叉搜索树中的节点
[<font style="color:#117CEE;">https://leetcode.cn/problems/delete-node-in-a-bst/description/](https://leetcode.cn/problems/delete-node-in-a-bst/description/)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713265211864-0a898c72-7c4b-4dab-a84f-3f711b77890b.png)

```java
public TreeNode deleteNode(TreeNode root, int key) {
    if (root == null) {
        return null;
    }
    if (root.val > key) {
        root.left = deleteNode(root.left, key);
        return root;
    }
    if (root.val < key) {
        root.right = deleteNode(root.right, key);
        return root;
    }
    if (root.val == key) {
        if (root.left == null && root.right == null) {
            return null;
        }
        if (root.right == null) {
            return root.left;
        }
        if (root.left == null) {
            return root.right;
        }
        TreeNode successor = root.right;
        while (successor.left != null) {
            successor = successor.left;
        }
        root.right = deleteNode(root.right, successor.val);
        successor.right = root.right;
        successor.left = root.left;
        return successor;
    }
    return root;
}
```

优化版

```java
public TreeNode deleteNode(TreeNode root, int key) {
    // 从根节点开始递归删除
    return deleteHelper(root, key);
}

private TreeNode deleteHelper(TreeNode root, int key) {
    if (root == null) {
        return null; // 如果树为空，直接返回空
    }

    // 递归查找要删除的节点
    if (key < root.val) {
        root.left = deleteHelper(root.left, key);
    } else if (key > root.val) {
        root.right = deleteHelper(root.right, key);
    } else {
        // 找到要删除的节点
        if (root.left == null) {
            // 只有右子树，返回右子树
            return root.right;
        } else if (root.right == null) {
            // 只有左子树，返回左子树
            return root.left;
        } else {
            // 有两个子节点的情况
            TreeNode minNode = findMin(root.right); // 找到右子树中的最小节点
            root.val = minNode.val; // 替换当前节点的值
            root.right = deleteHelper(root.right, minNode.val); // 删除最小节点
        }
    }
    return root;
}

private TreeNode findMin(TreeNode node) {
    // 找到树中最小的节点
    while (node.left != null) {
        node = node.left;
    }
    return node;
}
```



#### 题目示例
##### 1、二叉搜索树的最近公共祖先
[https://leetcode.cn/problems/er-cha-sou-suo-shu-de-zui-jin-gong-gong-zu-xian-lcof/description/](https://leetcode.cn/problems/er-cha-sou-suo-shu-de-zui-jin-gong-gong-zu-xian-lcof/description/)

> <font style="color:rgb(38, 38, 38);">给定一个二叉搜索树, 找到该树中两个指定节点的最近公共祖先。
>
> 百度百科<font style="color:rgb(38, 38, 38);">中最近公共祖先的定义为：“对于有根树 T 的两个结点 p、q，最近公共祖先表示为一个结点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（**<font style="color:rgb(38, 38, 38);">一个节点也可以是它自己的祖先**<font style="color:rgb(38, 38, 38);">）。”
>
> <font style="color:rgb(38, 38, 38);">例如，给定如下二叉搜索树:  root = [6,2,8,0,4,7,9,null,null,3,5]
>
> ![](https://cdn.nlark.com/yuque/0/2022/png/179650/1654069072379-f6eff747-38d8-46f8-9ef0-b7fef0dda793.png)
>
> 
>
> 输入: root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8
>
> 输出: 6 
>
> 解释: 节点 2 和节点 8 的最近公共祖先是 6。
>



思路1：

From 李宏源

先中序遍历二叉搜索树，得到递增的遍历结果，进而得到p节点的值，到q节点的值的区间范围

再次先序遍历二叉搜索树，最先找到的在此区间的节点  就是最近公共祖先



思路2：

From 刘慧玲

先判断根节点和p节点以及q节点的值的关系

如果根节点在pq之间，说明根节点就是最近公共祖先

否则，递归处理根节点的左子树或右子树



思路3：

先找到p节点的所有祖先

再找到q节点的所有祖先

然后依次判断，离pq最近的公共祖先



分析：

先查找节点P的祖先，

从根节点开始遍历树，如果直接找到，结束  
如果当前节点大于P的值，继续去左子树找，反之去右子树找

当找到节点后，所有经过的节点就是它的祖先

用同样的方式查找节点Q的祖先，得出两者的公共祖先，最近公共祖先是最后出现的公共祖先

```java
 public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
    List<TreeNode> pRes = getAncestor(root,p);
    List<TreeNode> qRes = getAncestor(root,q);
    TreeNode lowest = null;
    for (int i = 0; i < pRes.size() && i < qRes.size(); i++) {
         //都是从根节点开始遍历  前面的路径应该相同
         if(pRes.get(i) == qRes.get(i)){
             lowest = pRes.get(i);
         }else{
             // 只要出现不同 代表上一个相同的节点是最近公共祖先
             // 直接退出循环返回结果
             break;
         }
    }
    return lowest;
}

// 获取遍历路径  即为所有祖先节点
// Ancestor/ˈænsestə(r)/
public List<TreeNode> getAncestor(TreeNode root,TreeNode p){
    List<TreeNode> result = new ArrayList<>();
    TreeNode node = root;
    while(node != p){
        result.add(node);
        if(p.val < node.val){
            node = node.left;
        }else{
            node = node.right;
        }
    }
    result.add(node);
    return result;
}
```



如果两次遍历可以获得结果，那么一次遍历是否可以？

从根节点遍历，把当前节点R 同时和P/Q进行比较  
如果R>P && R>Q，说明P和Q都在它的左子树上，继续遍历左子树  
如果R<P  && R<Q，说明P和Q都在它的右子树上，继续遍历右子树  
如果R在[P,Q] 区间内，代表一个在左，一个在右，此处开始分岔，R即为最近公共祖先

```java
public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
    TreeNode ancestor = root;
    while (true) {
        if (p.val < ancestor.val && q.val < ancestor.val) {
            ancestor = ancestor.left;
        } else if (p.val > ancestor.val && q.val > ancestor.val) {
            ancestor = ancestor.right;
        } else {
            break;
        }
    }
    return ancestor;
}
```



##### 2、二叉搜索树中第K小的元素
[https://leetcode.cn/problems/kth-smallest-element-in-a-bst/](https://leetcode.cn/problems/kth-smallest-element-in-a-bst/)

递归解法

使用一个全局变量 count 来记录还需要找到第几小的元素，使用 result 来保存找到的结果。通过中序遍历 BST，我们可以按顺序访问节点，当 count 减为 0 时，就找到了第 K 小的节点。

```java
int count;
int result;

public int kthSmallest(TreeNode root, int k) {
    count = k;
    inOrderTraversal(root);
    return result;
}

private void inOrderTraversal(TreeNode node) {
    if (node == null) return;

    // 中序遍历，先遍历左子树
    inOrderTraversal(node.left);

    // 处理当前节点
    count--; // 每访问一个节点，计数器减一
    if (count == 0) {
        result = node.val; // 找到第 K 小的节点
        return;
    }

    // 遍历右子树
    inOrderTraversal(node.right);
}
```

非递归解法

```java
public int kthSmallest(TreeNode root, int k) {
    Deque<TreeNode> stack = new ArrayDeque<TreeNode>();
    while (root != null || !stack.isEmpty()) {
        while (root != null) {
            stack.push(root);
            root = root.left;
        }
        root = stack.pop();
        --k;
        if (k == 0) {
            break;
        }
        root = root.right;
    }
    return root.val;
}
```



##### 3、合并多颗二叉搜索树
[https://leetcode.cn/problems/merge-bsts-to-create-single-bst/description/](https://leetcode.cn/problems/merge-bsts-to-create-single-bst/description/)

```java
    // 存储所有叶节点值的哈希集合
    HashSet<Integer> leaves = new HashSet<>();
    // 存储 (根节点值, 树) 键值对的哈希映射
    HashMap<Integer, TreeNode> candidates = new HashMap<>();
    // 在中序遍历中存储中序遍历上一个遍历到的值，便于检查严格单调性
    int prev = 0;

    public TreeNode canMerge(List<TreeNode> trees) {
        for (TreeNode tree : trees) {
            if (tree.left != null) {
                leaves.add(tree.left.val);
            }
            if (tree.right != null) {
                leaves.add(tree.right.val);
            }
            candidates.put(tree.val, tree);
        }
        for (TreeNode tree : trees) {
            // 寻找合并完成后的树的根节点
            if (!leaves.contains(tree.val)) {
                // 将其从哈希映射中移除
                candidates.remove(tree.val);
                // 从根节点开始进行遍历
                // 如果中序遍历有严格单调性，并且所有树的根节点都被遍历到，说明可以构造二叉搜索树
                prev = 0;
                return (dfs(tree) && candidates.isEmpty()) ? tree : null;
            }
        }
        return null;
    }

    // 中序遍历，返回值表示是否有严格单调性
    private boolean dfs(TreeNode tree) {
        if (tree == null) {
            return true;
        }
        // 如果遍历到叶节点，并且存在可以合并的树，那么就进行合并
        if (tree.left == null && tree.right == null && candidates.containsKey(tree.val)) {
            tree.left = candidates.get(tree.val).left;
            tree.right = candidates.get(tree.val).right;
            // 合并完成后，将树从哈希映射中移除，以便于在遍历结束后判断是否所有树都被遍历过
            candidates.remove(tree.val);
        }

        // 先遍历左子树
        if (!dfs(tree.left)) {
            return false;
        }
        // 再遍历当前节点
        if (tree.val <= prev) {
            return false;
        }
        prev = tree.val;
        // 最后遍历右子树
        return dfs(tree.right);
    }
```



##### 4、二叉搜索树的最小绝对差
[530. 二叉搜索树的最小绝对差 - 力扣（LeetCode）](https://leetcode.cn/problems/minimum-absolute-difference-in-bst/description/)

给你一个二叉搜索树的根节点 root ，返回 树中任意两不同节点值之间的最小差值。

差值是一个正数，其数值等于两值之差的绝对值。

```java
private List<Integer> values = new ArrayList<>();
public int getMinimumDifference(TreeNode root) {
    // 中序遍历 BST
    inorderTraversal(root);

    // 寻找最小差值
    int minDiff = Integer.MAX_VALUE;
    for (int i = 1; i < values.size(); i++) {
        minDiff = Math.min(minDiff, values.get(i) - values.get(i - 1));
    }

    return minDiff;
}

private void inorderTraversal(TreeNode node) {
    if (node == null) {
        return;
    }
    inorderTraversal(node.left);
    values.add(node.val);
    inorderTraversal(node.right);
}
```



迭代法

```java
public int getMinimumDifference(TreeNode root) {
    Stack<TreeNode> stack = new Stack<>();
    TreeNode current = root;
    Integer prev = null;
    int minDiff = Integer.MAX_VALUE;

    while (current != null || !stack.isEmpty()) {
        while (current != null) {
            stack.push(current);
            current = current.left;
        }
        current = stack.pop();

        if (prev != null) {
            minDiff = Math.min(minDiff, current.val - prev);
        }
        prev = current.val;
        current = current.right;
    }

    return minDiff;
}
```



##### 5、二叉搜索树中的众数
[501. 二叉搜索树中的众数 - 力扣（LeetCode）](https://leetcode.cn/problems/find-mode-in-binary-search-tree/description/)

给你一个含重复值的二叉搜索树（BST）的根节点 root ，找出并返回 BST 中的所有 众数（即，出现频率最高的元素）。

如果树中有不止一个众数，可以按 任意顺序 返回。

假定 BST 满足如下定义：

+ 结点左子树中所含节点的值 小于等于 当前节点的值
+ 结点右子树中所含节点的值 大于等于 当前节点的值
+ 左子树和右子树都是二叉搜索树

```java
private List<Integer> values = new ArrayList<>();

public int[] findMode(TreeNode root) {
    if (root == null) {
        return new int[0];
    }

    // 中序遍历 BST
    inorderTraversal(root);

    // 统计频率
    Map<Integer, Integer> frequencyMap = new HashMap<>();
    int maxFrequency = 0;

    for (int value : values) {
        int count = frequencyMap.getOrDefault(value, 0) + 1;
        frequencyMap.put(value, count);
        maxFrequency = Math.max(maxFrequency, count);
    }

    // 找出所有众数
    List<Integer> modes = new ArrayList<>();
    for (Map.Entry<Integer, Integer> entry : frequencyMap.entrySet()) {
        if (entry.getValue() == maxFrequency) {
            modes.add(entry.getKey());
        }
    }

    // 转换结果为数组
    int[] result = new int[modes.size()];
    for (int i = 0; i < modes.size(); i++) {
        result[i] = modes.get(i);
    }

    return result;
}

private void inorderTraversal(TreeNode node) {
    if (node == null) {
        return;
    }
    inorderTraversal(node.left);
    values.add(node.val);
    inorderTraversal(node.right);
}
```



##### 6、把二叉搜索树转换为累加树
[538. 把二叉搜索树转换为累加树 - 力扣（LeetCode）](https://leetcode.cn/problems/convert-bst-to-greater-tree/description/)

```java
private int sum = 0;
public TreeNode convertBST(TreeNode root) {
    // 从右到左的中序遍历
    traverse(root);
    return root;
}

private void traverse(TreeNode node) {
    if (node == null) {
        return;
    }
    // 先遍历右子树
    traverse(node.right);
    // 更新当前节点的值
    sum += node.val;
    node.val = sum;
    // 再遍历左子树
    traverse(node.left);
}
```



### 平衡二叉树 
平衡二叉树是一种特殊的二叉搜索树，它具有自平衡的特性，保持了树的左右子树高度相差不超过1的特性。这种平衡性质确保了在最坏情况下的插入、删除和查找操作的时间复杂度都为 O(log n)，其中 n 是树中节点的数量。

平衡二叉树的特点包括：

高度平衡：任意节点的左右子树高度差不超过1，确保树的高度近似于 log n，从而保证了插入、删除和查找等操作的高效性。

左右子树也是平衡二叉树：平衡二叉树的左右子树也是平衡二叉树，通过递归地保持平衡性。



平衡二叉树的实现有多种，其中最常见的是 AVL 树和红黑树。

AVL 树：AVL 树是一种严格的平衡二叉树，它通过在插入或删除节点后进行旋转操作来保持树的平衡性。具体来说，AVL 树中任意节点的左右子树高度差的绝对值不超过1。

红黑树：前面我们提到了红黑树，它也是一种平衡二叉树，但相对于 AVL 树来说，它更加灵活一些。红黑树通过节点的颜色属性和一些规则来保持平衡，确保了树的高度近似于 log n。



平衡二叉树常被用于需要高效的插入、删除和查找操作的场景，例如数据库系统中的索引结构和动态集合的实现。在选择平衡二叉树时，需要考虑具体应用场景和对性能的要求，以及对平衡性维护的成本。



#### AVL树
[https://www.cs.usfca.edu/~galles/visualization/AVLtree.html](https://www.cs.usfca.edu/~galles/visualization/AVLtree.html)

平衡二叉树，是一种特殊的二叉排序树，左子树和右子树的高度差不超过1。空树也是平衡二叉树。

因为由苏联的数学家Adelse-Velskil和Landis 在1962年提出，简称AVL树

平衡因子 BF (Balance Factor) = 二叉树左子树的深度 - 右子树的深度

所得结果应该是 -1 0 1 总之平衡因子的绝对值 <= 1

##### 1、判断一棵树是否是平衡二叉树
[<font style="color:#117CEE;">https://leetcode.cn/problems/balanced-binary-tree/description/](https://leetcode.cn/problems/balanced-binary-tree/description/)

```java
public boolean isBalanced(TreeNode root) {
    return recur(root) != -1;
}

private int recur(TreeNode root) {
    // 空树
    if (root == null) return 0;
    // 先判断子树是否平衡  -1代表不平衡
    int left = recur(root.left);
    if (left == -1) return -1;
    int right = recur(root.right);
    if (right == -1) return -1;

    if (Math.abs(left - right) < 2) {
        return Math.max(left, right) + 1;
    }
    return -1;
}
```

##### 2、插入新的结点后如何保持树的平衡
###### 将有序数组转换为二叉搜索树
[https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/description/](https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/description/)

```java
public TreeNode sortedArrayToBST(int[] nums) {
    if (nums == null || nums.length == 0) {
        return null;
    }
    return buildBST(nums, 0, nums.length - 1);
}

private TreeNode buildBST(int[] nums, int start, int end) {
    if (start > end) {
        return null;
    }
    int mid = (start + end) / 2;
    TreeNode root = new TreeNode(nums[mid]);

    root.left = buildBST(nums, start, mid - 1);
    root.right = buildBST(nums, mid + 1, end);

    return root;
}
```



###### 将二叉搜索树变平衡
[https://leetcode.cn/problems/balance-a-binary-search-tree/description/](https://leetcode.cn/problems/balance-a-binary-search-tree/description/)

给你一棵二叉搜索树，请你返回一棵 平衡后 的二叉搜索树，新生成的树应该与原来的树有着相同的节点值。如果有多种构造方法，请你返回任意一种。

如果一棵二叉搜索树中，每个节点的两棵子树高度差不超过 1 ，我们就称这棵二叉搜索树是 平衡的 。



中序结果构造法

Java版

```java
public TreeNode balanceBST(TreeNode root) {
    // Step 1: 中序遍历获取节点值数组
    List<Integer> nodes = new ArrayList<>();
    inorderTraversal(root, nodes);

    // Step 2: 通过数组构建平衡二叉搜索树
    return buildBalancedBST(nodes, 0, nodes.size() - 1);
}

private void inorderTraversal(TreeNode node, List<Integer> nodes) {
    if (node == null)
        return;
    inorderTraversal(node.left, nodes);
    nodes.add(node.val);
    inorderTraversal(node.right, nodes);
}

private TreeNode buildBalancedBST(List<Integer> nodes, int start, int end) {
    if (start > end)
        return null;

    int mid = start + (end - start) / 2;
    TreeNode root = new TreeNode(nodes.get(mid));
    root.left = buildBalancedBST(nodes, start, mid - 1);
    root.right = buildBalancedBST(nodes, mid + 1, end);

    return root;
}
```

C++版

```cpp
class Solution {
public:
    TreeNode* balanceBST(TreeNode* root) {
        // Step 1: 中序遍历获取节点值数组
        vector<int> nodes;
        inorderTraversal(root, nodes);

        // Step 2: 通过数组构建平衡二叉搜索树
        return buildBalancedBST(nodes, 0, nodes.size() - 1);
    }

    // 中序遍历获取有序节点值数组
    void inorderTraversal(TreeNode* node, vector<int>& nodes) {
        if (node == nullptr)
            return;
        inorderTraversal(node->left, nodes);
        nodes.push_back(node->val);
        inorderTraversal(node->right, nodes);
    }

    // 根据有序数组构建平衡BST
    TreeNode* buildBalancedBST(vector<int>& nodes, int start, int end) {
        if (start > end)
            return nullptr;

        // 选择中间元素作为根节点，避免整数溢出
        int mid = start + (end - start) / 2;
        TreeNode* root = new TreeNode(nodes[mid]);
        root->left = buildBalancedBST(nodes, start, mid - 1);
        root->right = buildBalancedBST(nodes, mid + 1, end);

        return root;
    }
};
```



旋转解法

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // 使用一个容器 存储节点以及高度
    unordered_map<TreeNode*, int> height;

    TreeNode* balanceBST(TreeNode* root) {
        // 一颗新的平衡二叉树
        TreeNode * newRoot = nullptr;

        // 非递归的先序遍历
        stack<TreeNode*> st;
        TreeNode * cur = root;
        while (cur != nullptr || !st.empty()) {
            if (cur != nullptr)
            {
                st.push(cur);
                // 将此节点插入到平衡二叉树中
                newRoot = insert(newRoot, cur -> val);
                cur = cur->left;
            }
            else
            {
                cur = st.top();
                st.pop();
                cur = cur -> right;
            }
        }

        return newRoot;
    }

    // 插入前 传入当前的根节点 和 要插入的节点值
    // 插入后返回新的根节点
    TreeNode * insert(TreeNode * root, int val)
    {
        // 如果本身为空 直接插入一个节点
        if (root == nullptr)
        {
            root = new TreeNode(val);
            height[root] = 1;
            return root;
        }

        // 如果根节点不为空 比较新插入的值 和 根节点的值相比 如果更大 那么在右子树 如果更小 那么在左子树
        TreeNode * node = root;
        int cmp = val - node -> val;
        if (cmp < 0)
        {
            // 左子树
            // 如果要插入到左子树中 就是递归调用当前函数 并且将处理结果赋给左子树
            node -> left = insert(node -> left, val);
            // 如果左右子树的高度差超过1 需要旋转
            // 如果插入到左子树 左子树可能变高 所以不需要求绝对值
            if (getHeight(node -> left) - getHeight(node -> right) > 1)
            {
                // 如果插入的节点在左子树的右边，需要先左旋再右旋
                // 要注意 左旋是针对左孩子旋转  右旋是针对自身
                if (val > node -> left -> val)
                {
                    // 左旋 针对左孩子 所以左孩子的根节点变化
                    node -> left = leftRotate(node -> left);
                }
                // 如果插入的节点在左子树的左边，直接右旋
                // 针对自身 所以自身的根节点变化
                node = rightRotate(node);
            }
        }
        else if (cmp > 0)
        {
            // 右子树
            node -> right = insert(node -> right, val);
            if (getHeight(node -> right) - getHeight(node -> left) > 1)
            {
                // 如果插入的节点在右子树的左边，需要先右旋再左旋
                // 要注意 右旋是针对右孩子旋转  左旋是针对自身
                if (val < node -> right -> val)
                {
                    node -> right = rightRotate(node -> right);
                }
                // 如果插入的节点在右子树的右边，直接左旋
                node = leftRotate(node);
            }
        }

        // 节点的高度需要更新
        updateHeight(node);
        return node;
    }

    // 计算新高度并更新
    void updateHeight(TreeNode * node)
    {
        int newHeight = max(getHeight(node -> left), getHeight(node -> right)) + 1;
        height[node] = newHeight;
    }

    // 获取节点的高度
    int getHeight(TreeNode * node)
    {
        auto it = height.find(node);
        if (it != height.end())
        {
            // 如果找到节点在map中存在 返回高度
            return it -> second;
        }
        return 0;
    }

    // 左旋
    TreeNode * leftRotate(TreeNode * node)
    {
        // 先拿到右孩子  以及右孩子的左孩子
        TreeNode * rightChild = node -> right;
        TreeNode * rightLeftChild = rightChild -> left;
        // 旋转 右孩子的左孩子是当前节点 当前节点的右孩子是右孩子的左孩子
        rightChild -> left = node;
        node -> right = rightLeftChild;

        // 节点的高度需要更新
        updateHeight(node);
        // 重新计算
        updateHeight(rightChild);

        // 返回新的根节点
        return rightChild;
    }

    // 右旋
    TreeNode * rightRotate(TreeNode * node)
    {
        // 先拿到左孩子 以及左孩子的右孩子
        TreeNode * leftChild = node -> left;
        TreeNode * leftRightChild = leftChild -> right;
        // 旋转 左孩子的右孩子是当前节点 当前节点的左孩子是左孩子的右孩子
        leftChild -> right = node;
        node -> left = leftRightChild;
        // 节点的高度需要更新
        updateHeight(node);
        updateHeight(leftChild);

        // 返回新的根节点
        return leftChild;
    }
};
```

##### 3、树的旋转
1） 左旋

当在右子树中添加结点，造成不平衡时

以根结点的右孩子为中心，将根结点向左旋转

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310482335-c03ff35f-931e-4025-afaf-fd9ce4c1884c.png)

复杂一点的例子如下

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310488191-877e9e3f-4606-4f75-8dd9-fe6a9a383159.png)

再复杂一点的例子如下

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310492466-9aa0db53-1834-41d4-b60a-309333a0dc92.png)

二叉树的左旋操作是指将一个节点的右子节点旋转到其父节点的位置上，同时该节点成为其右子节点的左子节点。

```java
public TreeNode leftRotate(TreeNode x) {
    // 如果节点 x 或者节点 x 的右子节点为空，无法进行左旋操作
    if (x == null || x.right == null) {
        return x;
    }

    // 将节点 y 指向 x 的右子节点
    TreeNode y = x.right;

    // 将 y 的左子节点变为 x 的右子节点
    x.right = y.left;
    
    // 如果 y 的左子节点非空，则将其父节点指向 x
    if (y.left != null) {
        y.left.parent = x;
    }

    // 将 x 的父节点指向 y 的父节点
    y.parent = x.parent;

    // 如果 x 是根节点，则将 y 设置为新的根节点
    if (x.parent == null) {
        // 修改根节点指针
        return y;
    } 
    // 如果 x 是其父节点的左子节点，则将 y 设置为其父节点的左子节点
    else if (x == x.parent.left) {
        x.parent.left = y;
    } 
    // 如果 x 是其父节点的右子节点，则将 y 设置为其父节点的右子节点
    else {
        x.parent.right = y;
    }

    // 将 x 设置为 y 的左子节点
    y.left = x;
    // 将 x 的父节点设置为 y
    x.parent = y;

    return y;
}
```

2） 右旋

当在左子树中添加结点，造成不平衡时

以根结点的左孩子为中心，将根结点向右旋转

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310500234-a7da7065-aeb4-4a95-adb6-83dce3551265.png)

复杂一点的例子

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310504549-df173c74-35df-43e3-bbd4-1d168b5939d0.png)

二叉树的右旋操作与左旋操作相反，是将一个节点的左子节点旋转到其父节点的位置上，同时该节点成为其左子节点的右子节点。

```java
   public TreeNode rightRotate(TreeNode y) {
        // 如果节点 y 或者节点 y 的左子节点为空，无法进行右旋操作
        if (y == null || y.left == null) {
            return y;
        }

        // 将节点 x 指向 y 的左子节点
        TreeNode x = y.left;

        // 将 x 的右子节点变为 y 的左子节点
        y.left = x.right;
        
        // 如果 x 的右子节点非空，则将其父节点指向 y
        if (x.right != null) {
            x.right.parent = y;
        }

        // 将 y 的父节点指向 x 的父节点
        x.parent = y.parent;

        // 如果 y 是根节点，则将 x 设置为新的根节点
        if (y.parent == null) {
            // 修改根节点指针
            return x;
        } 
        // 如果 y 是其父节点的右子节点，则将 x 设置为其父节点的右子节点
        else if (y == y.parent.right) {
            y.parent.right = x;
        } 
        // 如果 y 是其父节点的左子节点，则将 x 设置为其父节点的左子节点
        else {
            y.parent.left = x;
        }

        // 将 y 设置为 x 的右子节点
        x.right = y;
        // 将 y 的父节点设置为 x
        y.parent = x;

        return x;
    }
```

3） 左右旋转

怎么判断一颗树，需要左旋还是右旋，还是左右旋和右左旋？

> 先查看这颗树整体的偏坠情况，比如向左偏，再查看左子树的偏坠情况，比如向右偏，如果树和子树的偏坠方向不同，那么需要旋转两个方向，我们先把子树旋转成和整棵树一样的偏坠情况，再将整棵树进行旋转。
>

先左旋再右旋

![画板](https://cdn.nlark.com/yuque/0/2024/jpeg/179650/1713269367661-b3b237fb-4548-4385-b897-fdece7d6bc4e.jpeg)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310510743-f607d1e9-2c81-4f4e-be44-f2fe66df1767.png)

 

第一次左旋 是以15为中心 向左旋转 同时将15的左孩子 移动为10的右孩子  
第二次右旋 是以15为中心 向右旋转 将17及其孩子 移动为15的右子树

4） 右左旋转

先右旋再左旋

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310518467-6a56b4fd-a4d8-4df7-b514-689e9878ef53.png)

```java
    // 左旋操作
    public TreeNode leftRotate(TreeNode x) {
        TreeNode y = x.right;
        x.right = y.left;
        y.left = x;
        return y;
    }

    // 右旋操作
    public TreeNode rightRotate(TreeNode y) {
        TreeNode x = y.left;
        y.left = x.right;
        x.right = y;
        return x;
    }

    // 左右旋操作
    public TreeNode leftRightRotate(TreeNode x) {
        x.left = leftRotate(x.left);
        return rightRotate(x);
    }

    // 右左旋操作
    public TreeNode rightLeftRotate(TreeNode y) {
        y.right = rightRotate(y.right);
        return leftRotate(y);
    }
```

###### 代码实现
```java
import java.util.LinkedList;
import java.util.Queue;


public class BalancedBinaryTree {

    public static void main(String[] args){
        BalancedBinaryTree tree = new BalancedBinaryTree();
        tree.batchInsert(new int[]{8,6,3,4,5,20,15,23,28,1,2});
        tree.tierPrint();
    }

    private Node root;

    /**
     * 节点
     */
    private class Node{
        int data;   // 数据
        Node left;  // 左指针
        Node right; // 右指针

        private Node(int data) {
            this.data = data;
            this.left = null;
            this.right = null;
        }
    }

    /**
     * 右旋操作（左孩子的左子树插入节点）
     * @param p
     */
    private Node rightRotate(Node p){
        Node temp = p.left;    // temp指向p的左子树
        p.left = temp.right;   // p的左子树指向temp的右子树
        temp.right = p;
        return temp;
    }

    /**
     * 左旋操作（右孩子的右子树插入节点）
     * @param p
     */
    private Node leftRotate(Node p){
        Node temp = p.right;    // temp指向p的右子树
        p.right = temp.left;   // p的右子树指向temp的左子树
        temp.left = p;
        return temp;
    }

    /**
     * 先左旋再右旋（左孩子的右子树插入节点）
     * @param p
     */
    private Node leftRightRotate(Node p){
        p.left = leftRotate(p.left);
        return rightRotate(p);
    }

    /**
     * 先右旋再左旋（右孩子的左子树插入节点）
     * @param p
     */
    private Node rightLeftRotate(Node p){
        p.right = rightRotate(p.right);
        return leftRotate(p);
    }

    /**
     * 树高
     * @param node
     * @return
     */
    private int getDepth(Node node){
        if (node == null){
            return 0;
        }
        return Math.max(getDepth(node.left), getDepth(node.right))+1;
    }

    /**
     * 平衡因子(左高：>1 等高：0 右高：<-1)
     * @return
     */
    public int balanceFactor(Node node){
        if (node == null){
            return 0;
        }
        return getDepth(node.left) - getDepth(node.right);
    }

    /**
     * 插入
     * @param node
     * @param data
     */
    public Node insert(Node node, int data){
        Node newData = new Node(data);
        if (node == null){
            return newData;
        }
        if (data < node.data){
            node.left = insert(node.left, data);
        }else if (data > node.data){
            node.right = insert(node.right, data);
        }else{
            return node;
        }
        int bf = balanceFactor(node);

        if (bf > 1 && data < node.left.data){
            // LL
            System.out.println("LL" + data);
            return rightRotate(node);
        }else if (bf < -1 && data > node.right.data){
            // RR
            System.out.println("RR" + data);
            return leftRotate(node);
        }else if (bf > 1 && data > node.left.data){
            // LR
            System.out.println("LR" + data);
            return leftRightRotate(node);
        }else if (bf < -1 && data < node.right.data){
            // RL
            System.out.println("RL" + data);
            return rightLeftRotate(node);
        }
        return node;
    }

    /**
     * 批量插入
     * @param arr
     */
    public void batchInsert(int[] arr){
        for (int data : arr){
            root = insert(root, data);
        }
    }

    /**
     * 前序遍历
     */
    public void prePrint(){
        System.out.print("前序遍历\t");
        if (root != null){
            pre(root);
        }
        System.out.println();
    }

    private void pre(Node node){
        if (node != null) {
            System.out.print(node.data + "\t");
            pre(node.left);
            pre(node.right);
        }
    }

    /**
     * 中序遍历
     */
    public void midPrint(){
        System.out.print("中序遍历\t");
        if (root != null){
            mid(root);
        }
        System.out.println();
    }

    private void mid(Node node){
        if (node != null) {
            mid(node.left);
            System.out.print(node.data + "\t");
            mid(node.right);
        }
    }

    /**
     * 后序遍历
     */
    public void postPrint(){
        System.out.print("后序遍历\t");
        if (root != null){
            post(root);
        }
        System.out.println();
    }

    private void post(Node node){
        if (node != null) {
            post(node.left);
            post(node.right);
            System.out.print(node.data + "\t");
        }
    }

    /**
     * 层序遍历，利用队列先进先出
     */
    public void tierPrint(){
        if (root != null){
            Queue<Node> queue = new LinkedList<>();
            queue.add(root);
            System.out.print("层序遍历\t");
            while (!queue.isEmpty()){
                Node temp = queue.remove();
                System.out.print(temp.data + "\t");
                if (temp.left != null){
                    // 左节点不为空，放进队列
                    queue.add(temp.left);
                }
                if (temp.right != null){
                    // 右节点不为空，放进队列
                    queue.add(temp.right);
                }
            }
        }
    }

}
```



#### 红黑树
红黑树是一种自平衡的二叉搜索树，它在每个节点上增加了一个额外的属性来存储节点的颜色，可以是红色或黑色。红黑树具有以下性质：

+ 节点颜色：每个节点要么是红色，要么是黑色。
+ 根节点颜色：根节点必须是黑色。
+ 叶子节点颜色：叶子节点（NIL节点，即空节点）都是黑色。
+ 相邻节点颜色：不能有两个相邻的红色节点，即红色节点的子节点和父节点不能同时为红色。
+ 黑色节点路径：从任一节点到其每个叶子的所有路径都包含相同数目的黑色节点，称为黑色节点路径相等。

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310530739-40e41c97-c7ee-462c-87a1-f16c944520ef.png)

这些性质确保了红黑树的平衡性和搜索性能。因为红黑树保持了对数高度的平衡性质，所以在最坏情况下，其插入、删除和查找操作的时间复杂度均为 O(log n)，其中 n 是树中节点的数量。

红黑树常被用作动态集合的数据结构，例如在 Java中的 TreeMap 就是使用红黑树来实现的。在数据库和文件系统等应用中也经常使用红黑树来实现索引结构。

尽管红黑树的插入、删除和查找操作的时间复杂度相对较低，并且能够保持树的平衡，但是其实现相对复杂，需要维护各种性质，因此在实际应用中，除非对平衡性和性能有较高要求，否则可能会选择使用其他简单的数据结构。



特征： 从根结点到叶子结点的最长路径，不会超过最短路径的两倍。

> 有了上面的几个性质作为限制，即可避免二叉查找树退化成单链表的情况。
>
> 但是，仅仅避免这种情况还不够，这里还要考虑某个节点到其每个叶子节点路径长度的问题。
>
> 如果某些路径长度过长，那么，在对这些路径上的及诶单进行增删查操作时，效率也会大大降低。
>
> 这个时候性质4和性质5用途就凸显了，有了这两个性质作为约束，即可保证任意节点到其每个叶子节点路径最长不会超过最短路径的2倍。原因如下：
>
> 当某条路径最短时，这条路径必然都是由黑色节点构成。当某条路径长度最长时，这条路径必然是由红色和黑色节点相间构成（性质4限定了不能出现两个连续的红色节点）。而性质5又限定了从任一节点到其每个叶子节点的所有路径必须包含相同数量的黑色节点。
>
> 此时，在路径最长的情况下，路径上红色节点数量 = 黑色节点数量。该路径长度为两倍黑色节点数量，也就是最短路径长度的2倍。
>

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713256474137-9a5764c3-d4c0-42db-b9e1-06f07302a3ec.png)

上图画出了从根节点 M 出发的到其叶子节点的最长和最短路径。这里偷懒只画出了两条最长路径，实际上最长路径有4条，分别为：

M -> Q -> O -> N

M -> Q -> O -> p

M -> Q -> Y -> X

M -> Q -> Y -> Z

长度为4，最短路径为 M -> E，长度为2。最长路径的长度正好为最短路径长度的2倍。



当插入新结点使红黑树失去平衡时，通过两种方式恢复平衡：旋转和变色 （红变黑 黑变红）

可视化网站

[https://www.cs.usfca.edu/~galles/visualization/RedBlack.html](https://www.cs.usfca.edu/~galles/visualization/RedBlack.html)

****

##### 1、搜索操作
红黑树和 AVL 树在搜索操作上有一些相似之处，但也有一些不同之处。

相似之处包括：

时间复杂度：在平衡状态下，红黑树和 AVL 树的搜索操作都具有相同的时间复杂度，即 O(log n)，其中 n 是树中节点的数量。

搜索过程：在进行搜索时，都是从根节点开始，按照特定的规则向左或向右递归地搜索，直到找到目标值或者搜索到叶子节点为止。



不同之处包括：

平衡条件：AVL 树严格要求每个节点的左右子树高度之差不超过1，因此它在维护平衡性方面比较严格，可能需要频繁地进行旋转操作来调整树的结构；而红黑树则是通过更加宽松的平衡条件来保持树的平衡，它只要求树的黑高度保持平衡，因此旋转操作相对较少。

旋转操作：由于 AVL 树的平衡要求更严格，因此在插入或删除节点时可能需要执行更多的旋转操作来保持平衡；而红黑树由于平衡条件相对宽松，所以在插入或删除节点时可能需要执行较少的旋转操作。

实现复杂度：AVL 树的实现相对红黑树来说更加复杂，因为它需要维护更加严格的平衡条件，这导致 AVL 树的插入、删除等操作可能会比红黑树稍慢一些。



总的来说，红黑树和 AVL 树都是用于解决二叉搜索树退化问题的重要数据结构，它们在搜索操作上有着相似的时间复杂度，但在细节上有一些区别。选择使用哪种结构取决于具体的应用场景和性能要求。



##### 2、插入操作
约定 新插入的结点都设为红色的，可以简化树的平衡过程

假设要插入的结点是X 父结点是P 祖父结点是G 叔父结点是U

1）X是根结点  
放入根结点中，将颜色变为黑色

2）X的父结点是黑色的

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310560822-0190b097-c421-4fec-972c-0c917b246044.png)

3）X的父结点是红色的

a) 如果叔父结点U也是红色的，可以推断出祖父结点G必是黑色的

当增加新结点时 造成两个红色结点相邻 此时使用变色处理  
P和U由从红色变为黑色 G由黑色变为红色 如果G是根结点 再次恢复为黑色

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310567119-35889bc4-41f6-4184-ae45-3d2b996d88e3.png)

b) 如果叔父结点U是黑色的，并且X在左侧

以P为中心，向右旋转，G和U下移，此时如果P有右孩子，右孩子R移动到G的左孩子处  
将P变为黑色 将G变为红色

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310572340-2a470ab9-ce4d-4b8c-b42e-28951105aebc.png)

c) 如果叔父结点U是黑色的，并且X在右侧

先通过左旋 恢复成第二种情况 然后再右旋和变色

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310582373-4fcc928b-4a26-4943-be5b-b93ba56f6c3a.png)



举个例子

> 假设我们插入这些数据：12   23    34   40  45   67    78   89   90  100  110  120   130   140  
>

插入12，12为根节点，根节点一定为黑；插入23，符合红黑树的基本性质，无需做出调整

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825167213-538969dd-6388-4888-9279-a5c227eab318.png)

插入45

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825182000-429ed058-5280-4985-b358-dc10103c167d.png)

不满足红色节点一定有两个黑色子节点，对12 节点左旋，23变成根，颜色变为黑色，12原来为黑色，旋转后这条路径多了一个黑色节点，所以为了满足性质5，必须将其颜色换为红色。

插入34

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825207931-2d4b997c-d543-47ea-a239-d51496ab8f7d.png)

插入34，不满足红色节点一定有两个黑色子节点，所以将34的父节点和叔叔节点 涂成 黑色，祖父节点变成红色，但23是根，必须为黑色，所以如上图所示 23，12，45节点颜色为黑色

插入40

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825233054-4cd77d9f-e420-4d32-9383-fe65d3014e51.png)



:::tips
插入数据情况总结

如果插入的节点，父节点为红色，叔叔节点（插入节点的父节点的兄弟节点）为红色，那么 就要把父节点和叔叔节点涂成黑色，祖父节点涂成红色（但如果是根节点涂成黑色）。

如果插入的节点，父节点为红色，父节点是祖父节点的右支，叔叔节点为黑色，且

（1）要插入的节点为父节点的右支，那么对其 祖父节点左旋。

（2）要插入的节点为父节点的左支，那么 对父节点先右旋，然后按照旋转后的位置重新进行规则判断，接着对其祖父节点进行左旋。

如果插入的节点，父节点为红色，父节点是祖父节点的左支，叔叔节点为黑色，且

（1）要插入的节点为父节点的左支，那么对其 祖父节点右旋。

（2）要插入的节点为父节点的右支，那么 对父节点先左旋，然后按照旋转后的位置重新进行规则判断，接着对其祖父节点进行右旋。（相当于如果是<，那么先左后右，如果是>,那么先右后左）

:::



再举个例子

插入节点 11

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825846707-cb18d1e2-362f-40d1-9da9-f36d833c0848.png)

这种情况下，以被插入节点的爷爷节点为根节点的子树范围内，无法通过左旋、右旋、变色操作来保持该子树的黑高不变，就需要向上扩大调整范围。

调整方法： 将父亲节点和叔叔节点染黑。爷爷节点染红。然后把爷爷节点15当成被插入的节点进行修复。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825869579-eb130699-1b34-4b7b-9044-eaa08d804198.png)



<font style="color:rgb(77, 77, 77);">插入31

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825887612-4eb13fea-5285-4a0d-8544-c7c9b4cdeaf8.png)

出现了红红相连的情况

调整方法：将父亲节点染黑。爷爷节点染红。 对爷爷节点右旋。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825908921-391d62a2-b15c-4dbc-ba2a-036ab40733ed.png)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825913542-c7a51364-741a-4e16-89c5-d926397ec901.png)

插入34

父节点在爷爷节点左侧，子节点在父节点右侧（LR双红）

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825922694-a3341234-d2b1-41a1-a80a-d40ddd840578.png)

调整方法：对父亲节点左旋

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825959623-cb0e1521-942d-492e-9174-77cf2790ef8b.png)

插入59

父节点在爷爷节点右侧，子节点在父节点右侧（RR双红）

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825986594-6a0f7de9-303f-47e6-8bf6-03a73f78b4cf.png)

调整方法：将父亲节点染黑。爷爷节点染红。对爷爷节点左旋。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712826001205-a9c851b2-d27d-4053-962c-e3fa97d704ea.png)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712826007134-5e61f168-342f-43db-9d66-8c9a0799a18f.png)

插入57

父节点在爷爷节点右侧，子节点在父节点左侧（RL双红）

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712826031950-47438a22-d555-4c56-a2b5-5f99d8d9049c.png)

调整方法：对父亲节点右旋 

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712826053256-b8a45081-8384-40b9-8010-777e9bf1a65a.png)



##### 3、删除操作
如果被删除节点是叶子节点，可以直接删除。

如果删除节点是非叶子节点，那么需要找到他的前驱或后继节点，来替换掉被删除节点的键和值，再删除他的前驱或后继节点。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825471690-9c23cd90-d927-4c4b-a38f-a854031cc55f.png)



修复删除后的红黑树

1.被删除节点是红色

这种情况不影响红黑树的性质，可以直接删除

2.被删除节点是黑色

2.1. 兄弟节点是黑色，且兄弟节点有红色的子节点

2.1.1. 兄弟节点的左子节点是红色

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825555692-d33ab83e-db5e-4bab-9724-090f29d547c5.png)

此时父亲节点的右子树为空，左子树为ll（兄弟节点在父亲节点的左侧，兄弟节点的左子节点在兄弟节点的左侧 left-left），

调整方法：将兄弟节点的颜色染为原来父亲节点的颜色，再将兄弟节点的左子节点和父亲节点染为黑色， 再对父亲节点右旋。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825562295-6cef8bf1-0da3-49c1-a105-c46a965ea9f6.png)

 这样此子树的黑高就与删除前的黑高相等，此子树与删除前的子树相比，少了一个红色节点。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825570593-562c4cfe-b257-4b2d-8eef-ebd43a7432f9.png)



2.1.2. 兄弟节点的右子节点是红色

 此时父亲节点的右子树为空，左子树为lR（left-right）

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825600097-bcb989d5-0139-4e7f-a104-d3f950f11a00.png)

对兄弟节点进行左旋，并交换兄弟节点与兄弟节点的右子节点的颜色，

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825607395-6efc4802-1aae-4e6a-8de7-0350ba678be9.png)

就可转换为2.1.1 的情况

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825614295-2e6ddeff-d2f8-432c-9642-0a8ab8b6676c.png)

2.2.兄弟节点是黑色，且兄弟节点的子节点都为黑色。

这里要特别说明，空节点也是黑色节点



2.2.1. 父节点是红色

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825629407-fbb770a5-6994-425f-b858-b0f85accd967.png) 

 将父亲节点染为黑色，兄弟节点染为红色即可

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825636706-54b90f7e-ef39-466f-a815-4b9b1531ded5.png)

2.2.2. 父节点是黑色

这种情况下，被删除节点、父亲节点、和兄弟节点都是黑色，被删除节点被删除后，不能在以父节点为根节点的子树范围内，保持黑高不变，此子树黑高必须减一，再扩大调整的范围，向上继续修复红黑树。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825643134-c2f2a5a0-ff33-4de8-a711-f4231320838f.png)

具体操作为：将兄弟节点染为红色（以父节点为根节点的子树黑高减一），再以父亲节点为删除节点，修复红黑树。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825654612-4d6c2c95-9649-481f-9af3-32623867862f.png)

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825660261-41cd1f34-633b-408f-a5c0-79c66971220c.png)



2.3. 兄弟节点是红色

父节点必定为黑色（性质4），且必定有黑色子节点，否则黑高不平衡（性质5）

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825672018-0d06d6fc-f2e6-4ee8-bc09-a51910ff714d.png)

父亲节点染为红色，兄弟节点染为黑色，再对父亲节点右旋，

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825681370-b36f6948-4c78-4218-ae04-6b69c9271e10.png)

此时就转换成了2.2.1的情况

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1712825688727-e09b5102-3ebb-4659-885d-90f393f53a48.png)

:::tips
在红黑树中，删除节点可能导致违反红黑树的性质，因此需要进行一系列操作来修正树的结构，以保持其平衡。根据被删除节点的不同情况，可以总结如下红黑树删除情况：

1.  **情况1：被删除节点是红色节点的情况**： 
    - 这种情况下，删除节点不会违反红黑树的性质，因为被删除的节点是红色的，不会影响黑色节点的数量或者红黑树的黑高度。直接删除即可，不需要额外的操作。
2.  **情况2：被删除节点是黑色节点，且有一个红色子节点**： 
    - 这种情况下，直接用红色子节点替代被删除节点，并将红色子节点染黑即可。不会破坏红黑树的性质。
3.  **情况3：被删除节点是黑色节点，且没有红色子节点**： 
    - 这是红黑树删除操作中最复杂的情况之一，可能会导致红黑树的性质被破坏。需要通过旋转和重新着色来修正树的结构，以保持其平衡和红黑树性质： 
        * 如果被删除节点是根节点，则直接删除即可。
        * 如果被删除节点是其父节点的唯一子节点，则删除该节点后，调整其父节点的颜色，使其变为红色（如果父节点原来是黑色，则删除一个黑色节点会导致黑高度不一致）。
        * 如果被删除节点有一个兄弟节点： 
            + 如果兄弟节点是红色的，通过旋转和重新着色操作，将情况转化为兄弟节点是黑色的情况。
            + 如果兄弟节点是黑色的，并且兄弟节点的两个子节点都是黑色的： 
                - 将兄弟节点染红，将父节点设为新的当前节点，继续进行递归操作。
            + 如果兄弟节点是黑色的，并且兄弟节点有一个红色子节点和一个黑色子节点： 
                - 将兄弟节点的黑色子节点变为红色，进行适当的旋转和重新着色操作，以达到平衡。
4.  **情况4：删除的节点有两个子节点**： 
    - 这种情况下，通常会找到被删除节点的后继节点（即比被删除节点大的最小节点），将后继节点的值复制到被删除节点上，并删除后继节点。然后处理删除后继节点的情况，转化为情况1、2或3。

总的来说，红黑树的删除操作需要注意维护红黑树的性质，主要是通过旋转和重新着色来确保树的平衡和性质不被破坏。

:::



##### 4、HashMap
[https://www.yuque.com/yuqingne/qsrwqt/sf5vv5cdmelzm7ei?singleDoc#](https://www.yuque.com/yuqingne/qsrwqt/sf5vv5cdmelzm7ei?singleDoc#) 《HashMap》

散列表 / 哈希表  hash table

![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1691045945679-09f63031-5839-4171-ab28-6dec0b31a9af.jpeg)

如果更改了数组的大小，但不重新分配位置，会让原来的元素无法识别是否已经存储过，导致出现重复元素，是不能接受的。

![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1691046271746-6f66842c-5605-477d-95ca-a8c7d0ff1085.jpeg)

重新分配位置后，再次计算原来的元素，才能找到正确的位置，但是成本比较高。

![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1691045947585-63a7dbd6-1566-4266-a459-67c95762170b.jpeg)

快速判断出某个数值是否存在

如果只使用普通的数组存储，需要遍历全部元素



如果想要建立一种映射关系，将数值指定到数组中某个位置去存储

我们通过将22%7=1，来得到数据22在数组中的对应位置，如果是40，对应索引位置为5，如果是75，同样对应为5，可以将75链接到40后面，形成一种链表结构。



<"01",5>

<"02",7>

<"2班",2>

<"大数据",4>

如何区分key各不相同？



![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1689057447579-302ba8c0-cedd-48e4-9886-994747409f8f.jpeg)

因为计算hash值时，会调用类的hashcode()方法，如果自定义的类，重写了equals()方法，但没有重写hashcode()，可能会导致，相同的值对应的对象都存储到集合中，这是和设计逻辑相违背的，所以在重写时，要同时重写。



HashMap是数组+链表的底层结构

源码分析

```java
    public V put(K key, V value) {
        return putVal(hash(key), key, value, false, true);
    }
```



```java
static final int hash(Object key) {
    int h;
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);
}
```



```java
 final V putVal(int hash, K key, V value, boolean onlyIfAbsent,
                   boolean evict) {
        Node<K,V>[] tab; Node<K,V> p; int n, i;
        if ((tab = table) == null || (n = tab.length) == 0)
            n = (tab = resize()).length;
        if ((p = tab[i = (n - 1) & hash]) == null)
            tab[i] = newNode(hash, key, value, null);
        else {
            Node<K,V> e; K k;
            if (p.hash == hash &&
                ((k = p.key) == key || (key != null && key.equals(k))))
                e = p;
            else if (p instanceof TreeNode)
                e = ((TreeNode<K,V>)p).putTreeVal(this, tab, hash, key, value);
            else {
                for (int binCount = 0; ; ++binCount) {
                    if ((e = p.next) == null) {
                        p.next = newNode(hash, key, value, null);
                        if (binCount >= TREEIFY_THRESHOLD - 1) // -1 for 1st
                            treeifyBin(tab, hash);
                        break;
                    }
                    if (e.hash == hash &&
                        ((k = e.key) == key || (key != null && key.equals(k))))
                        break;
                    p = e;
                }
            }
            if (e != null) { // existing mapping for key
                V oldValue = e.value;
                if (!onlyIfAbsent || oldValue == null)
                    e.value = value;
                afterNodeAccess(e);
                return oldValue;
            }
        }
        ++modCount;
        if (++size > threshold)
            resize();
        afterNodeInsertion(evict);
        return null;
    }
```

HashMap中声明了一个静态内部类Node，是一个单链表。

可以看出，单链表和双链表的区别，在于是否有前置节点的地址 prev。

```java
static class Node<K,V> implements Map.Entry<K,V> {
        final int hash;
        final K key;
        V value;
        Node<K,V> next;

        Node(int hash, K key, V value, Node<K,V> next) {
            this.hash = hash;
            this.key = key;
            this.value = value;
            this.next = next;
        }

        public final K getKey()        { return key; }
        public final V getValue()      { return value; }
        public final String toString() { return key + "=" + value; }

        public final int hashCode() {
            return Objects.hashCode(key) ^ Objects.hashCode(value);
        }

        public final V setValue(V newValue) {
            V oldValue = value;
            value = newValue;
            return oldValue;
        }

        public final boolean equals(Object o) {
            if (o == this)
                return true;
            if (o instanceof Map.Entry) {
                Map.Entry<?,?> e = (Map.Entry<?,?>)o;
                if (Objects.equals(key, e.getKey()) &&
                    Objects.equals(value, e.getValue()))
                    return true;
            }
            return false;
        }
    }
```



![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1689059054471-71bc0fb3-d8e6-4347-a096-68bd6d557fac.jpeg)

hashmap中重要的数据结构是  transient Node<K,V>[] table;

对于单链表而言，链表的头节点代表链表

对于双链表而言，链表的头节点和尾节点代表链表

table中的每一个元素，Node node = table[0]，其实是存储在当前位置中的头节点



```java
 if ((tab = table) == null || (n = tab.length) == 0)
            n = (tab = resize()).length;


tab = table
tab == null
如果tab==null 不执行 || 后面的操作
n = tab.length
n == 0
```

先将table赋值给tab，然后比较tab是否为空，或者将tab的长度赋值为n，判断n是否为0。

此时需要扩容，扩容对应的方法叫resize()



```java
  if ((p = tab[i = (n - 1) & hash]) == null)
            tab[i] = newNode(hash, key, value, null);
```

```java
//n = tab.length  
i = (n - 1)
i & hash

Node<K,V> p = tab[i & hash]
if (p  == null)
```

先根据key，计算出hash，再根据 (len-1)&hash 计算出要存储的数组索引位置，根据索引判断，当前位置是否存在元素。

key -> hash -> (len-1)&hash 

如果计算出来的位置中，没有元素，直接创建一个Node，包含hash值、key值、value值，next为空（没有下一个元素），赋值给tab[i]



> 16
>
> len-1   1111
>
> hash   0101
>
> &        0101
>
>  
>
> len-1   1111  
hash   1111
>
> &       1111
>
> (len-1) & hash <=  (len-1)
>
> index [0,len-1]
>



```java
    	else {
            Node<K,V> e; K k;
            if (p.hash == hash &&
                ((k = p.key) == key || (key != null && key.equals(k))))
                e = p;
            else if (p instanceof TreeNode)
                e = ((TreeNode<K,V>)p).putTreeVal(this, tab, hash, key, value);
            else {
                for (int binCount = 0; ; ++binCount) {
                    if ((e = p.next) == null) {
                        p.next = newNode(hash, key, value, null);
                        if (binCount >= TREEIFY_THRESHOLD - 1) // -1 for 1st
                            treeifyBin(tab, hash);
                        break;
                    }
                    if (e.hash == hash &&
                        ((k = e.key) == key || (key != null && key.equals(k))))
                        break;
                    p = e;
                }
            }
            if (e != null) { // existing mapping for key
                V oldValue = e.value;
                if (!onlyIfAbsent || oldValue == null)
                    e.value = value;
                afterNodeAccess(e);
                return oldValue;
            }
        }
```



![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1689061232178-ff7c51c8-c575-4c56-85fb-4f9552e644db.jpeg)

```java
            Node<K,V> e; K k;
            if (p.hash == hash &&
                ((k = p.key) == key || (key != null && key.equals(k))))
                e = p;

			if (e != null) { // existing mapping for key
                V oldValue = e.value;
                if (!onlyIfAbsent || oldValue == null)
                    e.value = value;
                afterNodeAccess(e);
                return oldValue;
            }
```

HashMap是数组+链表+红黑树的结构，刚开始数组中存储的是链表，当链表中元素个数过多时，会转化为红黑树。当数据发生变化时，如果树中的节点个数变少，也会再转化为链表。但同一时间里数组中要么存储的是链表，要么存储的是红黑树。



HashMap的put操作，会先根据key值计算hash值，在此过程中，调用了key类型所对应的hashcode方法，hash值是为了计算出存储到数组中哪一个位置上，因为相同的key可以计算出相同的hash，就能快速找出数组中是否存储了这个key值。如果没有存储过，创建一个Node节点，存储当前计算出的 hash key value，如果已经存储过，取出其中的Node节点，比较此节点和我们传入的数据NewNode中的hash和key是否相等，如果hash和key都相等，代表是同一元素，此时是覆盖操作，只更新value的值，如果key不相等，创建新的NewNode节点，存储当前计算出的 hash key value，将此NewNode节点设置为原来存储的Node节点的next指针（将其建立前后指向的关系）。

![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/179650/1689125783342-e211c278-90cb-47c5-94b1-03cf218719d1.jpeg)



![](https://cdn.nlark.com/yuque/0/2023/png/179650/1689062408086-bdc933e7-a70d-4aae-afcf-a79142937a29.png)



##### 5、TreeMap
TreeMap 是 Java 中的一个基于红黑树（Red-Black tree）实现的有序映射（SortedMap）。它继承自 AbstractMap 类，并且实现了 NavigableMap 和 Cloneable 接口。TreeMap 中的键值对是按照键的自然顺序或者给定的比较器进行排序的。这使得 TreeMap 保持了键值对的有序性，可以根据键进行范围查找以及基于键的排序。

以下是 TreeMap 的一些重要特性和使用场景：

1.  **有序性：** TreeMap 中的元素是有序的，可以根据键的自然顺序或者提供的比较器进行排序。这使得 TreeMap 在需要按照键有序存储和检索数据时非常有用。 
2.  **基于红黑树：** TreeMap 的底层实现采用了红黑树数据结构，这是一种自平衡二叉查找树。红黑树的平衡性保证了在最坏情况下的时间复杂度为 O(log n)，这使得 TreeMap 在查找、插入和删除操作上具有较高的性能。 
3.  **支持键的范围操作：** TreeMap 提供了一系列方法，可以获取键的子集，如 headMap、tailMap 和 subMap，这些方法可以根据指定的范围获取键的子集合，非常方便。 
4.  **线程不安全：** TreeMap 是非同步的，不支持多线程并发操作。如果需要在多线程环境下使用，可以通过 Collections.synchronizedSortedMap 方法将 TreeMap 转换为同步的 SortedMap。 
5.  **适用于排序和范围查找：** 由于 TreeMap 的有序性和支持范围操作的特性，它非常适用于需要按照键排序和进行范围查找的场景，比如实现字典、排行榜、范围查询等。 

总的来说，TreeMap 是一个高效的有序映射实现，适用于需要按照键排序和进行范围操作的场景，它提供了丰富的方法和特性来操作和管理键值对数据。

```java
import java.util.*;

public class TreeMapTest {
    public static void main(String[] args) {
        // Creating a TreeMap
        TreeMap<Integer, String> treeMap = new TreeMap<>();

        // Adding elements to the TreeMap
        treeMap.put(3, "Apple");
        treeMap.put(2, "Banana");
        treeMap.put(1, "Orange");
        treeMap.put(4, "Grapes");

        // Displaying the TreeMap
        System.out.println("TreeMap: " + treeMap);

        // Accessing elements in TreeMap
        System.out.println("Value associated with key 2: " + treeMap.get(2));

        // Removing an element from TreeMap
        treeMap.remove(1);
        System.out.println("After removing key 1: " + treeMap);

        // Checking if a key is present
        System.out.println("Is key 3 present? " + treeMap.containsKey(3));

        // Checking if a value is present
        System.out.println("Is value 'Grapes' present? " + treeMap.containsValue("Grapes"));

        // Iterating over TreeMap using entrySet
        System.out.println("Iterating over TreeMap using entrySet:");
        for (Map.Entry<Integer, String> entry : treeMap.entrySet()) {
            System.out.println("Key: " + entry.getKey() + ", Value: " + entry.getValue());
        }

        // Iterating over TreeMap using keySet
        System.out.println("Iterating over TreeMap using keySet:");
        for (Integer key : treeMap.keySet()) {
            System.out.println("Key: " + key + ", Value: " + treeMap.get(key));
        }

        // Iterating over TreeMap using values
        System.out.println("Iterating over TreeMap using values:");
        for (String value : treeMap.values()) {
            System.out.println("Value: " + value);
        }

        // Getting the first and last key in the TreeMap
        System.out.println("First key: " + treeMap.firstKey());
        System.out.println("Last key: " + treeMap.lastKey());

        // Getting the portion of TreeMap whose keys are less than 3
        SortedMap<Integer, String> headMap = treeMap.headMap(3);
        System.out.println("Portion of TreeMap whose keys are less than 3: " + headMap);

        // Getting the portion of TreeMap whose keys are greater than or equal to 2
        SortedMap<Integer, String> tailMap = treeMap.tailMap(2);
        System.out.println("Portion of TreeMap whose keys are greater than or equal to 2: " + tailMap);

        // Getting the portion of TreeMap whose keys are between 1 (inclusive) and 3 (exclusive)
        SortedMap<Integer, String> subMap = treeMap.subMap(1, 3);
        System.out.println("Portion of TreeMap whose keys are between 1 and 3: " + subMap);

        // Clearing the TreeMap
        treeMap.clear();
        System.out.println("TreeMap after clearing: " + treeMap);
    }
}
```

TreeMap 的查找操作流程和二叉查找树一致。二叉树的查找流程是这样的，先将目标值和根节点的值进行比较，如果目标值小于根节点的值，则再和根节点的左孩子进行比较。如果目标值大于根节点的值，则继续和根节点的右孩子比较。在查找过程中，如果目标值和二叉树中的某个节点值相等，则返回 true，否则返回 false。TreeMap 查找和此类似，只不过在 TreeMap 中，节点（Entry）存储的是键值对<k,v>。在查找过程中，比较的是键的大小，返回的是值，如果没找到，则返回null。TreeMap 中的查找方法是get，具体实现在getEntry方法中。

```java
public V get(Object key) {
    Entry<K,V> p = getEntry(key);
    return (p==null ? null : p.value);
}

final Entry<K,V> getEntry(Object key) {
    // Offload comparator-based version for sake of performance
    if (comparator != null)
        return getEntryUsingComparator(key);
    if (key == null)
        throw new NullPointerException();
    @SuppressWarnings("unchecked")
        Comparable<? super K> k = (Comparable<? super K>) key;
    Entry<K,V> p = root;
    
    // 查找操作的核心逻辑就在这个 while 循环里
    while (p != null) {
        int cmp = k.compareTo(p.key);
        if (cmp < 0)
            p = p.left;
        else if (cmp > 0)
            p = p.right;
        else
            return p;
    }
    return null;
}

```

当往 TreeMap 中放入新的键值对后，可能会破坏红黑树的性质。这里为了描述方便，把 Entry 称为节点。并把新插入的节点称为N，N 的父节点为P。P 的父节点为G，且 P 是 G 的左孩子。P 的兄弟节点为U。在往红黑树中插入新的节点 N 后（新节点为红色），会产生下面5种情况：

N 是根节点

N 的父节点是黑色

N 的父节点是红色，叔叔节点也是红色

N 的父节点是红色，叔叔节点是黑色，且 N 是 P 的右孩子

N 的父节点是红色，叔叔节点是黑色，且 N 是 P 的左孩子

上面5种情况中，情况2不会破坏红黑树性质，所以无需处理。情况1 会破坏红黑树性质2（根是黑色），情况3、4、和5会破坏红黑树性质4（每个红色节点必须有两个黑色的子节点）。这个时候就需要进行调整，以使红黑树重新恢复平衡。

```java
public V put(K key, V value) {
    Entry<K,V> t = root;
    // 1.如果根节点为 null，将新节点设为根节点
    if (t == null) {
        compare(key, key);
        root = new Entry<>(key, value, null);
        size = 1;
        modCount++;
        return null;
    }
    int cmp;
    Entry<K,V> parent;
    // split comparator and comparable paths
    Comparator<? super K> cpr = comparator;
    if (cpr != null) {
        // 2.为 key 在红黑树找到合适的位置
        do {
            parent = t;
            cmp = cpr.compare(key, t.key);
            if (cmp < 0)
                t = t.left;
            else if (cmp > 0)
                t = t.right;
            else
                return t.setValue(value);
        } while (t != null);
    } else {
        // 与上面代码逻辑类似，省略
    }
    Entry<K,V> e = new Entry<>(key, value, parent);
    // 3.将新节点链入红黑树中
    if (cmp < 0)
        parent.left = e;
    else
        parent.right = e;
    // 4.插入新节点可能会破坏红黑树性质，这里修正一下
    fixAfterInsertion(e);
    size++;
    modCount++;
    return null;
}
```

插入逻辑的复杂之处在于插入后的修复操作，对应的方法fixAfterInsertion。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713229031215-1e70a12a-8829-4a2d-852b-f7a094f14a20.png)



fixAfterInsertion 方法的实现基于红黑树的插入修正算法，其主要步骤如下：

设置新插入节点的颜色为红色。

在一个循环中，根据插入节点的父节点和祖父节点的颜色进行不同的修正操作，直到红黑树的性质得到满足。

+ 如果父节点是祖父节点的左子节点：
    - 如果叔叔节点（祖父节点的右子节点）是红色，进行颜色调整，并将 x 移动到祖父节点。
    - 如果叔叔节点是黑色，并且插入节点是父节点的右子节点，进行左旋操作，并调整颜色。
    - 如果叔叔节点是黑色，并且插入节点是父节点的左子节点，进行右旋操作，并调整颜色。
+ 如果父节点是祖父节点的右子节点，与上述操作对称。

最后，将根节点的颜色设置为黑色，以满足红黑树性质。



删除操作是红黑树最复杂的部分，原因是该操作可能会破坏红黑树性质5（从任一节点到其每个叶子的所有简单路径都包含相同数目的黑色节点），修复性质5要比修复其他性质（性质2和4需修复，性质1和3不用修复）复杂的多。当删除操作导致性质5被破坏时，会出现8种情况。为了方便表述，这里还是先做一些假设。我们把最终被删除的节点称为 X，X 的替换节点称为 N。N 的父节点为P，且 N 是 P 的左孩子。N 的兄弟节点为S，S 的左孩子为 SL，右孩子为 SR。这里特地强调 X 是 最终被删除 的节点，是原因二叉查找树会把要删除有两个孩子的节点的情况转化为删除只有一个孩子的节点的情况，该节点是欲被删除节点的前驱和后继。

接下来，简单列举一下删除节点时可能会出现的情况，先列举较为简单的情况：

最终被删除的节点 X 是红色节点

X 是黑色节点，但该节点的孩子节点是红色

比较复杂的情况：

替换节点 N 是新的根

N 为黑色，N 的兄弟节点 S 为红色，其他节点为黑色。

N 为黑色，N 的父节点 P，兄弟节点 S 和 S 的孩子节点均为黑色。

N 为黑色，P 是红色，S 和 S 孩子均为黑色。

N 为黑色，P 可红可黑，S 为黑色，S 的左孩子 SL 为红色，右孩子 SR 为黑色

N 为黑色，P 可红可黑，S 为黑色，SR 为红色，SL 可红可黑

上面列举的8种情况中，前两种处理起来比较简单，后6种情况中情况26较为复杂。

```java
public V remove(Object key) {
    Entry<K,V> p = getEntry(key);
    if (p == null)
        return null;

    V oldValue = p.value;
    deleteEntry(p);
    return oldValue;
}

private void deleteEntry(Entry<K,V> p) {
    modCount++;
    size--;

    /* 
     * 1. 如果 p 有两个孩子节点，则找到后继节点，
     * 并把后继节点的值复制到节点 P 中，并让 p 指向其后继节点
     */
    if (p.left != null && p.right != null) {
        Entry<K,V> s = successor(p);
        p.key = s.key;
        p.value = s.value;
        p = s;
    } // p has 2 children

    // Start fixup at replacement node, if it exists.
    Entry<K,V> replacement = (p.left != null ? p.left : p.right);

    if (replacement != null) {
        /*
         * 2. 将 replacement parent 引用指向新的父节点，
         * 同时让新的父节点指向 replacement。
         */ 
        replacement.parent = p.parent;
        if (p.parent == null)
            root = replacement;
        else if (p == p.parent.left)
            p.parent.left  = replacement;
        else
            p.parent.right = replacement;

        // Null out links so they are OK to use by fixAfterDeletion.
        p.left = p.right = p.parent = null;

        // 3. 如果删除的节点 p 是黑色节点，则需要进行调整
        if (p.color == BLACK)
            fixAfterDeletion(replacement);
    } else if (p.parent == null) { // 删除的是根节点，且树中当前只有一个节点
        root = null;
    } else { // 删除的节点没有孩子节点
        // p 是黑色，则需要进行调整
        if (p.color == BLACK)
            fixAfterDeletion(p);

        // 将 P 从树中移除
        if (p.parent != null) {
            if (p == p.parent.left)
                p.parent.left = null;
            else if (p == p.parent.right)
                p.parent.right = null;
            p.parent = null;
        }
    }
}
```

核心实现在deleteEntry方法中。deleteEntry 主要做了这么几件事：

如果待删除节点 P 有两个孩子，则先找到 P 的后继 S，然后将 S 中的值拷贝到 P 中，并让 P 指向 S

如果最终被删除节点 P（P 现在指向最终被删除节点）的孩子不为空，则用其孩子节点替换掉

如果最终被删除的节点是黑色的话，调用 fixAfterDeletion 方法进行修复

上面说了 replacement 不为空时，deleteEntry 的执行逻辑。上面说的略微啰嗦，如果简单说的话，7个字即可总结：找后继 -> 替换 -> 修复。这三步中，最复杂的是修复操作。



fixAfterDeletion() 方法的主要作用是修正红黑树的性质，以保持树的平衡。其实现基于红黑树的删除修正算法，步骤如下：

进入一个循环，直到当前节点不是根节点且其颜色为黑色为止。

根据当前节点的位置，分为两种情况处理：

+ 如果当前节点是父节点的左子节点：
    - 获取当前节点的兄弟节点。
    - 如果兄弟节点是红色，进行颜色调整和旋转操作，使得兄弟节点变为黑色。
    - 如果兄弟节点和其子节点都是黑色，将兄弟节点设置为红色，将当前节点指向其父节点。
    - 如果兄弟节点右子节点是黑色，左子节点是红色，进行颜色调整和旋转操作，将左子节点变为当前节点的兄弟节点。
    - 进行最终的颜色调整和旋转操作，以确保树的平衡。
+ 如果当前节点是父节点的右子节点，与上述情况对称处理。

将最终的当前节点的颜色设置为黑色，以确保红黑树的性质得到满足。

fixAfterDeletion() 方法通过颜色调整和旋转操作，确保在删除节点后，红黑树的性质得到保持和修正，从而保证树的平衡和高效性能。

![](https://cdn.nlark.com/yuque/0/2024/png/179650/1713229130350-251288b1-9db4-45d2-a2bc-9ae6c073bd6c.png)

###### HashMap与TreeMap区别
:::tips
HashMap和TreeMap都是Java中常用的Map实现类，它们之间的主要区别在于内部数据结构和性能特点：

内部数据结构：

+ HashMap：HashMap基于哈希表实现，使用哈希函数将键映射到存储桶（buckets）中，并在存储桶中使用链表或红黑树（Java 8及以后版本）来处理键的冲突。在JDK 8之前，HashMap使用链表来解决冲突；而在JDK 8及以后版本，当链表长度超过一定阈值时，HashMap会将链表转换为红黑树，以提高查找、插入和删除的性能。
+ TreeMap：TreeMap基于红黑树实现，它是一种自平衡的二叉搜索树，保持所有键值对的有序状态。每个节点包含键值对以及指向左右子节点的指针，红黑树的平衡性质确保了树的高度较低，因此查找、插入和删除操作的时间复杂度是O(log n)。



性能特点：

+ HashMap：查找、插入和删除操作的平均时间复杂度为O(1)，但在最坏情况下可能达到O(n)，取决于哈希函数的质量和键的分布情况。HashMap适用于快速的查找操作，尤其是当哈希函数的质量较高且键的分布均匀时，性能较好。
+ TreeMap：查找、插入和删除操作的时间复杂度为O(log n)，因为红黑树的自平衡性质保证了树的高度较低。TreeMap适用于需要有序遍历键值对的场景，以及需要对键进行范围查询的场景，但在大多数情况下，它的性能略低于HashMap。



有序性：

+ HashMap：HashMap不保证键值对的顺序，键值对的顺序可能会随着时间的变化而变化。
+ TreeMap：TreeMap保持所有键值对的有序状态，键值对按照键的自然顺序或者通过Comparator进行排序。

综上所述，HashMap适用于需要快速查找操作且不需要保持有序状态的场景，而TreeMap适用于需要有序遍历键值对或者进行范围查询的场景。

:::



##### 6、代码实现
```java
public class RBTree<T extends Comparable<T>> {

    private RBTNode<T> mRoot;    // 根结点

    private static final boolean RED   = false;
    private static final boolean BLACK = true;

    public class RBTNode<T extends Comparable<T>> {
        boolean color;        // 颜色
        T key;                // 关键字(键值)
        RBTNode<T> left;    // 左孩子
        RBTNode<T> right;    // 右孩子
        RBTNode<T> parent;    // 父结点

        public RBTNode(T key, boolean color, RBTNode<T> parent, RBTNode<T> left, RBTNode<T> right) {
            this.key = key;
            this.color = color;
            this.parent = parent;
            this.left = left;
            this.right = right;
        }

    }

    ...
}
```

左旋

```java
/* 
 * 对红黑树的节点(x)进行左旋转
 *
 * 左旋示意图(对节点x进行左旋)：
 *      px                              px
 *     /                               /
 *    x                               y                
 *   /  \      --(左旋)-.           / \                #
 *  lx   y                          x  ry     
 *     /   \                       /  \
 *    ly   ry                     lx  ly  
 *
 *
 */
private void leftRotate(RBTNode<T> x) {
    // 设置x的右孩子为y
    RBTNode<T> y = x.right;

    // 将 “y的左孩子” 设为 “x的右孩子”；
    // 如果y的左孩子非空，将 “x” 设为 “y的左孩子的父亲”
    x.right = y.left;
    if (y.left != null)
        y.left.parent = x;

    // 将 “x的父亲” 设为 “y的父亲”
    y.parent = x.parent;

    if (x.parent == null) {
        this.mRoot = y;            // 如果 “x的父亲” 是空节点，则将y设为根节点
    } else {
        if (x.parent.left == x)
            x.parent.left = y;    // 如果 x是它父节点的左孩子，则将y设为“x的父节点的左孩子”
        else
            x.parent.right = y;    // 如果 x是它父节点的左孩子，则将y设为“x的父节点的左孩子”
    }
    
    // 将 “x” 设为 “y的左孩子”
    y.left = x;
    // 将 “x的父节点” 设为 “y”
    x.parent = y;
}

```



```java
/* 
 * 对红黑树的节点(y)进行右旋转
 *
 * 右旋示意图(对节点y进行左旋)：
 *            py                               py
 *           /                                /
 *          y                                x                  
 *         /  \      --(右旋)-.            /  \                     #
 *        x   ry                           lx   y  
 *       / \                                   / \                   #
 *      lx  rx                                rx  ry
 * 
 */
private void rightRotate(RBTNode<T> y) {
    // 设置x是当前节点的左孩子。
    RBTNode<T> x = y.left;

    // 将 “x的右孩子” 设为 “y的左孩子”；
    // 如果"x的右孩子"不为空的话，将 “y” 设为 “x的右孩子的父亲”
    y.left = x.right;
    if (x.right != null)
        x.right.parent = y;

    // 将 “y的父亲” 设为 “x的父亲”
    x.parent = y.parent;

    if (y.parent == null) {
        this.mRoot = x;            // 如果 “y的父亲” 是空节点，则将x设为根节点
    } else {
        if (y == y.parent.right)
            y.parent.right = x;    // 如果 y是它父节点的右孩子，则将x设为“y的父节点的右孩子”
        else
            y.parent.left = x;    // (y是它父节点的左孩子) 将x设为“x的父节点的左孩子”
    }

    // 将 “y” 设为 “x的右孩子”
    x.right = y;

    // 将 “y的父节点” 设为 “x”
    y.parent = x;
}

```



```java
/* 
 * 将结点插入到红黑树中
 *
 * 参数说明：
 *     node 插入的结点        // 对应《算法导论》中的node
 */
private void insert(RBTNode<T> node) {
    int cmp;
    RBTNode<T> y = null;
    RBTNode<T> x = this.mRoot;

    // 1. 将红黑树当作一颗二叉查找树，将节点添加到二叉查找树中。
    while (x != null) {
        y = x;
        cmp = node.key.compareTo(x.key);
        if (cmp < 0)
            x = x.left;
        else
            x = x.right;
    }

    node.parent = y;
    if (y!=null) {
        cmp = node.key.compareTo(y.key);
        if (cmp < 0)
            y.left = node;
        else
            y.right = node;
    } else {
        this.mRoot = node;
    }

    // 2. 设置节点的颜色为红色
    node.color = RED;

    // 3. 将它重新修正为一颗二叉查找树
    insertFixUp(node);
}

```



```java
/* 
 * 新建结点(key)，并将其插入到红黑树中
 *
 * 参数说明：
 *     key 插入结点的键值
 */
public void insert(T key) {
    RBTNode<T> node=new RBTNode<T>(key,BLACK,null,null,null);

    // 如果新建结点失败，则返回。
    if (node != null)
        insert(node);
}
```



```java
/*
 * 红黑树插入修正函数
 *
 * 在向红黑树中插入节点之后(失去平衡)，再调用该函数；
 * 目的是将它重新塑造成一颗红黑树。
 *
 * 参数说明：
 *     node 插入的结点        // 对应《算法导论》中的z
 */
private void insertFixUp(RBTNode<T> node) {
    RBTNode<T> parent, gparent;

    // 若“父节点存在，并且父节点的颜色是红色”
    while (((parent = parentOf(node))!=null) && isRed(parent)) {
        gparent = parentOf(parent);

        //若“父节点”是“祖父节点的左孩子”
        if (parent == gparent.left) {
            // Case 1条件：叔叔节点是红色
            RBTNode<T> uncle = gparent.right;
            if ((uncle!=null) && isRed(uncle)) {
                setBlack(uncle);
                setBlack(parent);
                setRed(gparent);
                node = gparent;
                continue;
            }

            // Case 2条件：叔叔是黑色，且当前节点是右孩子
            if (parent.right == node) {
                RBTNode<T> tmp;
                leftRotate(parent);
                tmp = parent;
                parent = node;
                node = tmp;
            }

            // Case 3条件：叔叔是黑色，且当前节点是左孩子。
            setBlack(parent);
            setRed(gparent);
            rightRotate(gparent);
        } else {    //若“z的父节点”是“z的祖父节点的右孩子”
            // Case 1条件：叔叔节点是红色
            RBTNode<T> uncle = gparent.left;
            if ((uncle!=null) && isRed(uncle)) {
                setBlack(uncle);
                setBlack(parent);
                setRed(gparent);
                node = gparent;
                continue;
            }

            // Case 2条件：叔叔是黑色，且当前节点是左孩子
            if (parent.left == node) {
                RBTNode<T> tmp;
                rightRotate(parent);
                tmp = parent;
                parent = node;
                node = tmp;
            }

            // Case 3条件：叔叔是黑色，且当前节点是右孩子。
            setBlack(parent);
            setRed(gparent);
            leftRotate(gparent);
        }
    }

    // 将根节点设为黑色
    setBlack(this.mRoot);
}

```



```java
/* 
 * 删除结点(node)，并返回被删除的结点
 *
 * 参数说明：
 *     node 删除的结点
 */
private void remove(RBTNode<T> node) {
    RBTNode<T> child, parent;
    boolean color;

    // 被删除节点的"左右孩子都不为空"的情况。
    if ( (node.left!=null) && (node.right!=null) ) {
        // 被删节点的后继节点。(称为"取代节点")
        // 用它来取代"被删节点"的位置，然后再将"被删节点"去掉。
        RBTNode<T> replace = node;

        // 获取后继节点
        replace = replace.right;
        while (replace.left != null)
            replace = replace.left;

        // "node节点"不是根节点(只有根节点不存在父节点)
        if (parentOf(node)!=null) {
            if (parentOf(node).left == node)
                parentOf(node).left = replace;
            else
                parentOf(node).right = replace;
        } else {
            // "node节点"是根节点，更新根节点。
            this.mRoot = replace;
        }

        // child是"取代节点"的右孩子，也是需要"调整的节点"。
        // "取代节点"肯定不存在左孩子！因为它是一个后继节点。
        child = replace.right;
        parent = parentOf(replace);
        // 保存"取代节点"的颜色
        color = colorOf(replace);

        // "被删除节点"是"它的后继节点的父节点"
        if (parent == node) {
            parent = replace;
        } else {
            // child不为空
            if (child!=null)
                setParent(child, parent);
            parent.left = child;

            replace.right = node.right;
            setParent(node.right, replace);
        }

        replace.parent = node.parent;
        replace.color = node.color;
        replace.left = node.left;
        node.left.parent = replace;

        if (color == BLACK)
            removeFixUp(child, parent);

        node = null;
        return ;
    }

    if (node.left !=null) {
        child = node.left;
    } else {
        child = node.right;
    }

    parent = node.parent;
    // 保存"取代节点"的颜色
    color = node.color;

    if (child!=null)
        child.parent = parent;

    // "node节点"不是根节点
    if (parent!=null) {
        if (parent.left == node)
            parent.left = child;
        else
            parent.right = child;
    } else {
        this.mRoot = child;
    }

    if (color == BLACK)
        removeFixUp(child, parent);
    node = null;
}

/* 
 * 删除结点(z)，并返回被删除的结点
 *
 * 参数说明：
 *     tree 红黑树的根结点
 *     z 删除的结点
 */
public void remove(T key) {
    RBTNode<T> node; 

    if ((node = search(mRoot, key)) != null)
        remove(node);
}

```



```java
/*
 * 红黑树删除修正函数
 *
 * 在从红黑树中删除插入节点之后(红黑树失去平衡)，再调用该函数；
 * 目的是将它重新塑造成一颗红黑树。
 *
 * 参数说明：
 *     node 待修正的节点
 */
private void removeFixUp(RBTNode<T> node, RBTNode<T> parent) {
    RBTNode<T> other;

    while ((node==null || isBlack(node)) && (node != this.mRoot)) {
        if (parent.left == node) {
            other = parent.right;
            if (isRed(other)) {
                // Case 1: x的兄弟w是红色的  
                setBlack(other);
                setRed(parent);
                leftRotate(parent);
                other = parent.right;
            }

            if ((other.left==null || isBlack(other.left)) &&
                (other.right==null || isBlack(other.right))) {
                // Case 2: x的兄弟w是黑色，且w的俩个孩子也都是黑色的  
                setRed(other);
                node = parent;
                parent = parentOf(node);
            } else {

                if (other.right==null || isBlack(other.right)) {
                    // Case 3: x的兄弟w是黑色的，并且w的左孩子是红色，右孩子为黑色。  
                    setBlack(other.left);
                    setRed(other);
                    rightRotate(other);
                    other = parent.right;
                }
                // Case 4: x的兄弟w是黑色的；并且w的右孩子是红色的，左孩子任意颜色。
                setColor(other, colorOf(parent));
                setBlack(parent);
                setBlack(other.right);
                leftRotate(parent);
                node = this.mRoot;
                break;
            }
        } else {

            other = parent.left;
            if (isRed(other)) {
                // Case 1: x的兄弟w是红色的  
                setBlack(other);
                setRed(parent);
                rightRotate(parent);
                other = parent.left;
            }

            if ((other.left==null || isBlack(other.left)) &&
                (other.right==null || isBlack(other.right))) {
                // Case 2: x的兄弟w是黑色，且w的俩个孩子也都是黑色的  
                setRed(other);
                node = parent;
                parent = parentOf(node);
            } else {

                if (other.left==null || isBlack(other.left)) {
                    // Case 3: x的兄弟w是黑色的，并且w的左孩子是红色，右孩子为黑色。  
                    setBlack(other.right);
                    setRed(other);
                    leftRotate(other);
                    other = parent.left;
                }

                // Case 4: x的兄弟w是黑色的；并且w的右孩子是红色的，左孩子任意颜色。
                setColor(other, colorOf(parent));
                setBlack(parent);
                setBlack(other.left);
                rightRotate(parent);
                node = this.mRoot;
                break;
            }
        }
    }

    if (node!=null)
        setBlack(node);
}
```



### 赫夫曼树
#### 1、认识概念
也叫哈夫曼树，还叫最优二叉树。

认识一些概念：

1） 结点之间的路径

指从一个结点到另一个结点所经过的所有结点。  
A-H的路径，是ABDH

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310304020-ba479589-381e-46f5-8a31-5e1f567d0d22.png)

2） 路径长度

指的是，从一个结点到另一个结点所经过的“边”的数量。  
A-H的路径长度，是 AB + BD + DH三条边，即为3。

3）带权路径长度

权重，指一个结点的重要程度，有一个数值。  
带权路径长度 指的是，从根结点到当前结点的路径长度，乘以当前结点的权重，所得乘积。  
假设H的权重是3，H的带权路径长度 = H的路径长度 * H的权重 = 3 * 3 = 9

4） 树的带权路径长度

指的是，叶子结点的带权路径长度之和

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310311955-31769010-b194-42a2-a3d3-860ae62524bf.png)

图中的树的带权路径长度为 3*3 + 6*3 + 1*2 + 4*2 + 8*2 = 9 + 18 + 2 + 8 + 16 = 53

简称为 WPL = Weighted Path Length of Tree

赫夫曼树的概念：树的带权路径长度最小的二叉树

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310319780-2df12d7f-49e3-427b-94a2-ea629914022a.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310324414-68e30d2c-5297-466c-8987-fb70163dca5d.png)



#### 2、构造逻辑
借助有序队列，不断从队列中取出最小的两个结点，然后求出结点之和，作为新的节点重新存入队列中，存入时保证队列有序。当队列中只剩余一个值时，构造完成。

构造出来的树的特点，权重大的结点在上层，权重小的结点在底层，以保证叶子节点的带权路径长度尽可能的小，最终整棵树的带权路径长度也最小。

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310381955-f53e0877-2d07-4bba-8732-d3985be53740.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310376997-9f54a19d-770f-4bae-bc8f-2d20907ddc54.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310391567-9566a23d-9666-4d4d-80b8-267aeb4228a0.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310396867-d1318faa-198d-4697-843c-2f66f4bbd28f.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310402609-6ccac875-105b-42f5-8d6c-624f1c16fca6.png)

#### 3、代码实现
```java
import java.util.PriorityQueue;

class HuffmanNode {
    int weight;
    HuffmanNode left;
    HuffmanNode right;

    public HuffmanNode(int weight) {
        this.weight = weight;
        this.left = null;
        this.right = null;
    }
}

public class HuffmanTreeBuilder {
    public HuffmanNode buildHuffmanTree(int[] frequencies) {
        // 创建优先队列，并按照频率将叶子节点插入队列
        PriorityQueue<HuffmanNode> pq = new PriorityQueue<>((a, b) -> a.weight - b.weight);
        for (int freq : frequencies) {
            pq.offer(new HuffmanNode(freq));
        }

        // 依次从队列中取出两个权重最小的节点，合并成新节点，直到队列中只剩下一个节点
        while (pq.size() > 1) {
            HuffmanNode left = pq.poll();
            HuffmanNode right = pq.poll();
            HuffmanNode parent = new HuffmanNode(left.weight + right.weight);
            parent.left = left;
            parent.right = right;
            pq.offer(parent);
        }

        // 返回哈夫曼树的根节点
        return pq.poll();
    }

    public static void main(String[] args) {
        int[] frequencies = {5, 9, 12, 13, 16, 45};
        HuffmanTreeBuilder builder = new HuffmanTreeBuilder();
        HuffmanNode root = builder.buildHuffmanTree(frequencies);
        // 这里可以添加遍历哈夫曼树的代码，验证树的构建是否正确
    }
}
```



### 堆
概念： 是一颗完全二叉树 每一个都大于/小于其左右节点

如果根节点保存的是最大值，称为大根堆  
如果根节点保存的是最小值，称为小根堆

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310621087-78c76173-5dd0-48a2-ac6f-a437996ca33d.png)



堆（Heap）是一种特殊的树形数据结构，通常用数组来实现。堆分为两种主要类型：最大堆和最小堆。堆具有以下特点：

1.  **父节点与子节点的大小关系**：在最大堆中，父节点的值大于或等于其子节点的值；而在最小堆中，父节点的值小于或等于其子节点的值。 
2.  **树形结构**：堆通常是一个完全二叉树，即除了最底层之外，其他层的节点都被完全填充，并且最底层的节点都尽可能地靠左排列。 
3.  **基于数组的实现**：堆通常是用数组来实现的，因为数组在内存中是连续存储的，可以更有效地利用缓存，而且通过下标可以快速访问到父节点和子节点。 

堆常见的操作包括插入元素、删除元素和获取堆顶元素等。这些操作都可以在对数时间内完成。

+  **插入元素**：将新元素插入到堆的末尾，然后通过上浮操作（如果是最小堆）或下沉操作（如果是最大堆）将其调整到合适的位置，以满足堆的性质。 
+  **删除元素**：通常是删除堆顶元素。为了维护堆的性质，需要将堆顶元素移除，并将堆中的最后一个元素移到堆顶，然后通过下沉操作（最小堆）或上浮操作（最大堆）将其调整到合适的位置。 
+  **获取堆顶元素**：堆顶元素即为堆中具有最大值（最大堆）或最小值（最小堆）的元素，可以在O(1)时间内完成获取操作。 

堆在算法设计中有着广泛的应用，其中最常见的就是优先队列（Priority Queue）的实现。优先队列是一种特殊的队列，它允许你按照优先级来取出元素，而堆就是实现优先队列的一种常见方式。除此之外，堆还被用来解决诸如堆排序、Dijkstra 算法、最小生成树算法（如 Prim 和 Kruskal 算法）等各种算法问题。



#### 基本操作
##### 1、插入操作
先找到满足完全二叉树特性时，新元素应该插入的位置，不断和父节点比较，当比父节点元素值大时，上浮，不断重复，直到找到自身位置

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310625712-2ae299f8-b2cc-4657-bcf0-1c071c3d1e8b.png)



##### 2、删除操作
删除大根堆的最大值，根节点（62）删除后，用叶子节点中最右侧节点进行填充 （16）

填充后从上往下遍历，查找左右节点中的更大值，进行替换，此过程也叫下沉

16 -> 41 16->28 16->19 得到最终结果

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310630426-d56f6057-db78-4331-95a2-05e6afca6162.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310635053-453d7025-ecdc-4e07-afba-e5dcbfda3425.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310640985-74bf8ce4-75aa-4fc6-975c-8cdf15162965.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310645920-d99d820b-2963-430a-a607-78c281f2ba49.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310652736-00b36e66-2e35-4836-a894-1587403d4af8.png)

如果插入新元素和删除最大元素同时发生，新元素直接替代根节点的位置，然后开始下沉

堆的操作时间复杂度为 O(logN)

构造堆 [15, 11,19, 12, 22, 26, 28, 30, 41, 62]

先构造完全二叉树，然后依次处理叶子节点，判断是否比父节点大，如果叶子节点更大，则上浮，直到找到合适的位置

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310662122-1a4cbfd4-8451-483a-97ac-aabc4752bac5.png)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310668303-f1276a65-f416-40ca-b197-849dd168a4f6.png)



最小堆如下

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310674809-3555db66-d8e2-4a27-b37d-0c35bdf60536.png)

##### 3、代码实现
```java
public class MaxHeap {
    private int[] heap; // 用于存储堆的数组
    private int size; // 堆中的元素数量

    public MaxHeap(int[] sortedArray) {
        this.size = sortedArray.length;
        this.heap = new int[size + 1]; // 留出一个额外的位置，避免数组下标从0开始时的混淆

        // 将有序数组复制到堆数组中
        for (int i = 0; i < size; i++) {
            heap[i + 1] = sortedArray[i];
        }

        // 构造最大堆
        buildMaxHeap();
    }

    // 构造最大堆的方法，从最后一个非叶子节点向前进行下沉操作
    private void buildMaxHeap() {
        for (int i = size / 2; i >= 1; i--) {
            maxHeapify(i);
        }
    }

    // 下沉操作，确保堆的性质
    private void maxHeapify(int index) {
        int leftChild = 2 * index; // 左子节点
        int rightChild = 2 * index + 1; // 右子节点
        int largest = index;

        // 比较左子节点和父节点的值
        if (leftChild <= size && heap[leftChild] > heap[largest]) {
            largest = leftChild;
        }

        // 比较右子节点和当前最大值的节点
        if (rightChild <= size && heap[rightChild] > heap[largest]) {
            largest = rightChild;
        }

        // 如果父节点不是最大的值，则交换并继续下沉操作
        if (largest != index) {
            swap(index, largest);
            maxHeapify(largest);
        }
    }

    // 交换两个位置的值
    private void swap(int index1, int index2) {
        int temp = heap[index1];
        heap[index1] = heap[index2];
        heap[index2] = temp;
    }

    // 返回堆中的元素
    public int[] getHeap() {
        int[] result = new int[size];
        System.arraycopy(heap, 1, result, 0, size);
        return result;
    }
}
```

#### 题目示例
##### 1、优先级队列
PriorityQueue和Comparator比较器

```java
public class PriorityQueueTest {

    public static void main(String[] args) {
//        PriorityQueue<String> queue = new PriorityQueue<>();
//        queue.offer("1");
//        queue.offer("4");
//        queue.offer("3");
//        queue.offer("2");
//
//        System.out.println(queue.poll());
//        System.out.println(queue.poll());
//        System.out.println(queue.poll());
//        System.out.println(queue.poll());

        // 自定义排序
        PriorityQueue<Student> studentQueue = new PriorityQueue<>(
                new Comparator<Student>() {
                    @Override
                    public int compare(Student o1, Student o2) {
                        // 按分数排序  分数相同  按名字排序
                        if (o1.score == o2.score) {
                            return o1.name.compareTo(o2.name);
                        }
                        return o2.score - o1.score;
                    }
                });
        studentQueue.offer(new Student("atuo", 90));
        studentQueue.offer(new Student("dmc", 60));
        studentQueue.offer(new Student("bo", 60));
        studentQueue.offer(new Student("qing", 0));

        System.out.println(studentQueue.poll());
        System.out.println(studentQueue.poll());
        System.out.println(studentQueue.poll());
        System.out.println(studentQueue.poll());
    }
}

class Student {
    String name;
    int score;

    public Student(String name, int score) {
        this.name = name;
        this.score = score;
    }

    @Override
    public String toString() {
        return "Student{" +
                "name='" + name + '\'' +
                ", score=" + score +
                '}';
    }
}
```



##### 2、最小k个数
[https://leetcode.cn/problems/smallest-k-lcci/description/](https://leetcode.cn/problems/smallest-k-lcci/description/)

> <font style="color:rgb(99, 99, 99);">设计一个算法，找出数组中最小的k个数。以任意顺序返回这k个数均可。
>
> 示例：
>
> 输入： arr = [1,3,5,7,2,4,6,8], k = 4
>
> 输出： [1,2,3,4]
>

```java
public int[] smallestK(int[] arr, int k) {
    int[] result = new int[k];
    PriorityQueue<Integer> queue = new PriorityQueue();
    for (int i = 0; i < arr.length; i++) {
        queue.offer(arr[i]);
    }
    for (int i = 0; i < k; i++) {
        result[i] = queue.poll();
    }
    return result;
}
```



### 线段树
<font style="color:rgb(36, 41, 47);">线段树（Segment Tree）是一种用于高效处理区间查询的数据结构，主要用于解决区间问题，如区间最值、区间和等。它在一维情况下将整个区间划分成一系列不相交的子区间，每个子区间对应线段树中的一个节点，而每个节点都存储了该子区间的一些信息，例如区间的最小值、最大值、和等。通过这种方式，线段树使得对区间的查询可以在对数时间内完成。

<font style="color:rgb(36, 41, 47);">

<font style="color:rgb(36, 41, 47);">线段树的主要特点包括：

<font style="color:rgb(36, 41, 47);">递归分治结构<font style="color:rgb(36, 41, 47);">：线段树的构建和查询过程都是基于递归的分治思想。通过将区间逐步分解为更小的子区间，并在树中的节点上存储这些子区间的信息，实现了高效的区间查询。

<font style="color:rgb(36, 41, 47);">空间效率高<font style="color:rgb(36, 41, 47);">：虽然线段树在树形结构中存储了大量的信息，但由于其严格的分治结构，其空间复杂度为O(n)，其中n是待处理区间的长度。

<font style="color:rgb(36, 41, 47);">查询效率高<font style="color:rgb(36, 41, 47);">：线段树可以在对数时间内完成各种类型的区间查询操作，如区间最值、区间和等。

<font style="color:rgb(36, 41, 47);">支持动态更新<font style="color:rgb(36, 41, 47);">：线段树支持动态更新操作，例如修改某个区间内的元素值，以及增加或删除某个元素。

<font style="color:rgb(36, 41, 47);">线段树通常用于解决一维的区间查询问题，但也可以扩展到高维情况下。它在各种领域中都有广泛的应用，如算法竞赛、信息检索、图像处理等。



序列 A = [1,4,2,3]

1. 给序列的第i个数 加上X A[i] = A[i] + X O(1)
2. 取序列中最大的数 遍历取最大值 O(N)

“区间”，线段树是根据区间的性质来构造的

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310603882-bbeafca2-7558-4ec8-9326-1fdd7b1813ed.png)

特点：

每次将区间的长度一分为二，区间存储左右边界 [start, end]

如果假设数组的长度为n，线段树的高度 log(n)

将区间中的最大值加入进来，线段树进化成如下状态

除此之外，可以存储区间内的最小值，区间求和等等

线段树的节点个数为 n + n/2 + n/4 + ... = (1 + 1/2 + 1/4 + ... ) * n = 2n

构造线段树的时间复杂度和空间复杂度 均为 O(n)

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310610942-861639ef-71e5-49e2-99ec-abe69a708fd5.png)

线段树的单点更新

![](https://cdn.nlark.com/yuque/0/2023/png/179650/1691310615868-8f4c76d8-0608-4971-8b1c-71e6974137f8.png)



#### 核心思想
线段树（Segment Tree）是一种高效处理**区间查询**和**区间更新**的数据结构，通过将数组划分为若干子区间（线段），实现对任意区间的快速操作。它广泛应用于区间求和、区间最值、区间修改等场景，是解决数组动态操作问题的核心工具之一。

线段树是一种**二叉树**，每个节点代表数组的一个**连续子区间**。其结构特点：

+ 根节点代表整个数组（区间 `[0, n-1]`）。
+ 每个非叶节点将自身区间**平分**为左右两个子区间，分别由左右子节点表示。
+ 叶节点对应数组中的单个元素（区间长度为 1）。

例如，对于数组 `[1, 3, 5, 7, 9, 11]`，线段树的结构如下：

```plain
          [0-5]
         /     \
    [0-2]       [3-5]
   /   \        /   \
[0-1] [2-2] [3-4] [5-5]
 /  \        /  \
[0] [1]    [3] [4]
```

****

**核心操作**

线段树的核心操作包括**构建树**、**区间查询**和**区间更新**，均通过递归实现，时间复杂度为 `O(log n)`（`n` 为数组长度）。



构建树（Build）

+ 递归将区间划分为左右子区间，直到叶节点（单个元素）。
+ 非叶节点的值由左右子节点的值合并得到（如求和、取最值等）。

```cpp
// 构建线段树（以区间和为例）
void build(int node, int start, int end) {
    if (start == end) {
        // 叶节点：存储数组元素
        tree[node] = arr[start];
    } else {
        int mid = (start + end) / 2;
        int left = 2 * node + 1;   // 左子节点索引
        int right = 2 * node + 2;  // 右子节点索引
        build(left, start, mid);   // 构建左子树
        build(right, mid+1, end); // 构建右子树
        tree[node] = tree[left] + tree[right]; // 合并左右子树（求和）
    }
}
```



区间查询（Query）

+ 查询指定区间 `[l, r]` 的聚合结果（如和、最值等）。
+ 递归判断当前节点区间与查询区间的关系：

```cpp
// 查询区间和 [l, r]
int query(int node, int start, int end, int l, int r) {
    if (r < start || end < l) {
        // 无重叠：返回0
        return 0;
    }
    if (l <= start && end <= r) {
        // 完全重叠：返回当前节点值
        return tree[node];
    }
    // 部分重叠：递归查询左右子树
    int mid = (start + end) / 2;
    int left = 2 * node + 1;
    int right = 2 * node + 2;
    int sumLeft = query(left, start, mid, l, r);
    int sumRight = query(right, mid+1, end, l, r);
    return sumLeft + sumRight; // 合并结果
}
```

>   完全重叠：直接返回当前节点的值。
>
>   部分重叠：递归查询左右子树，合并结果。
>
>   不重叠：返回无效值（如 0 for 求和，±∞ for 最值）。
>



区间更新（Update）

+ 支持**单点更新**（修改单个元素）和**区间批量更新**（如给区间 `[l, r]` 所有元素加 `val`）。
+ 区间批量更新需通过**懒标记（Lazy Propagation）** 优化，避免每次更新都递归到叶节点：

```cpp
// 懒标记更新（区间加 val）
void updateRange(int node, int start, int end, int l, int r, int val) {
    if (lazy[node] != 0) {
        // 先处理当前节点的懒标记
        tree[node] += (end - start + 1) * val;
        if (start != end) {
            // 非叶节点：将标记传递给子节点
            lazy[2*node+1] += val;
            lazy[2*node+2] += val;
        }
        lazy[node] = 0; // 清空当前节点标记
    }

    if (r < start || end < l) return; // 无重叠

    if (l <= start && end <= r) {
        // 完全重叠：更新当前节点并标记子节点（若有）
        tree[node] += (end - start + 1) * val;
        if (start != end) {
            lazy[2*node+1] += val;
            lazy[2*node+2] += val;
        }
        return;
    }

    // 部分重叠：递归更新左右子树
    int mid = (start + end) / 2;
    updateRange(2*node+1, start, mid, l, r, val);
    updateRange(2*node+2, mid+1, end, l, r, val);
    tree[node] = tree[2*node+1] + tree[2*node+2]; // 合并结果
}
```

懒标记记录 “待更新” 的操作，暂时不向下传递。

当需要访问子节点时，再将标记传递给子节点并更新其值。



**关键特性**

1. **时间复杂度**：构建树 `O(n)`，查询和更新 `O(log n)`（得益于二叉树的分治特性）。
2. **空间复杂度**：`O(4n)`（为保证完全二叉树结构，通常开原数组 4 倍大小的空间）。
3. **灵活性**：支持多种聚合操作（求和、最值、计数、GCD 等），只需修改合并逻辑。
4. **懒标记**：是区间批量更新的核心优化，避免重复操作，确保效率。

**适用场景**

+ 区间求和、区间平均值、区间最大 / 最小值查询。
+ 单点修改、区间批量增减、区间覆盖（如将 `[l, r]` 所有元素改为 `val`）。
+ 动态数据统计（如实时更新的销售数据、温度监测等）。

**优缺点**

+ **优点**：功能全面，支持几乎所有区间操作；效率稳定，适合大规模数据。
+ **缺点**：实现较复杂（尤其是懒标记）；空间消耗较大（4n）。

**总结**

线段树是处理区间问题的 “万能工具”，通过二叉树划分区间和懒标记优化，实现了高效的查询与更新。尽管实现稍复杂，但其灵活性和效率使其在算法竞赛、数据分析等领域不可或缺。



#### 1、区域和检索
[https://leetcode-cn.com/problems/range-sum-query-mutable/](https://leetcode-cn.com/problems/range-sum-query-mutable/)

NumArray 类有一个私有数组 nums 用于存储原始数据，一个私有数组 segmentTree 用于存储线段树，以及一个整数 n 表示数组的长度。

在构造函数 NumArray(int[] nums) 中，我们首先构建线段树。

buildSegmentTree 方法用于构建线段树，递归地将当前区间划分为两个子区间，并在每个节点存储子区间的和。

update 方法用于更新 nums 数组和线段树中的值。

sumRange 方法用于计算 left 和 right 之间的区间和，它也是一个递归方法，根据查询区间与当前节点的关系，选择向左子树、右子树或两者都查找。

```java
class NumArray {
    private int[] nums;
    private int[] segmentTree;
    private int n;

    public NumArray(int[] nums) {
        this.nums = nums;
        this.n = nums.length;
        this.segmentTree = new int[4 * n]; // 线段树的大小为4倍的原数组大小
        buildSegmentTree(0, 0, n - 1);
    }

    private void buildSegmentTree(int treeIndex, int left, int right) {
        if (left == right) {
            segmentTree[treeIndex] = nums[left];
            return;
        }

        int mid = left + (right - left) / 2;
        int leftChild = 2 * treeIndex + 1;
        int rightChild = 2 * treeIndex + 2;

        buildSegmentTree(leftChild, left, mid);
        buildSegmentTree(rightChild, mid + 1, right);

        segmentTree[treeIndex] = segmentTree[leftChild] + segmentTree[rightChild];
    }

    public void update(int index, int val) {
        update(0, 0, n - 1, index, val);
    }

    private void update(int treeIndex, int left, int right, int index, int val) {
        if (left == right && left == index) {
            nums[index] = val;
            segmentTree[treeIndex] = val;
            return;
        }

        int mid = left + (right - left) / 2;
        int leftChild = 2 * treeIndex + 1;
        int rightChild = 2 * treeIndex + 2;

        if (index <= mid) {
            update(leftChild, left, mid, index, val);
        } else {
            update(rightChild, mid + 1, right, index, val);
        }

        segmentTree[treeIndex] = segmentTree[leftChild] + segmentTree[rightChild];
    }

    public int sumRange(int left, int right) {
        return sumRange(0, 0, n - 1, left, right);
    }

    private int sumRange(int treeIndex, int treeLeft, int treeRight, int left, int right) {
        if (treeLeft == left && treeRight == right) {
            return segmentTree[treeIndex];
        }

        int mid = treeLeft + (treeRight - treeLeft) / 2;
        int leftChild = 2 * treeIndex + 1;
        int rightChild = 2 * treeIndex + 2;

        if (right <= mid) {
            return sumRange(leftChild, treeLeft, mid, left, right);
        } else if (left > mid) {
            return sumRange(rightChild, mid + 1, treeRight, left, right);
        } else {
            int leftSum = sumRange(leftChild, treeLeft, mid, left, mid);
            int rightSum = sumRange(rightChild, mid + 1, treeRight, mid + 1, right);
            return leftSum + rightSum;
        }
    }
}
```



树状数组解法

C++版

```cpp
class NumArray {
private:
    vector<int> nums;
    vector<int> tree;

    int prefixSum(int i) {
        int s = 0;
        for (; i > 0; i &= i - 1) { // i -= i & -i 的另一种写法
            s += tree[i];
        }
        return s;
    }

public:
    NumArray(vector<int> &nums) : nums(nums.size()), tree(nums.size() + 1) {
        for (int i = 0; i < nums.size(); i++) {
            update(i, nums[i]);
        }
    }

    void update(int index, int val) {
        int delta = val - nums[index];
        nums[index] = val;
        for (int i = index + 1; i < tree.size(); i += i & -i) {
            tree[i] += delta;
        }
    }

    int sumRange(int left, int right) {
        return prefixSum(right + 1) - prefixSum(left);
    }
};
```



#### 2、水果成篮 III
[https://leetcode.cn/problems/fruits-into-baskets-iii/description/](https://leetcode.cn/problems/fruits-into-baskets-iii/description/)

给你两个长度为 n 的整数数组，fruits 和 baskets，其中 fruits[i] 表示第 i 种水果的 数量，baskets[j] 表示第 j 个篮子的 容量。

你需要对 fruits 数组从左到右按照以下规则放置水果：

每种水果必须放入第一个 容量大于等于 该水果数量的 最左侧可用篮子 中。

每个篮子只能装 一种 水果。

如果一种水果 无法放入 任何篮子，它将保持 未放置。

返回所有可能分配完成后，剩余未放置的水果种类的数量。



示例 1

输入： fruits = [4,2,5], baskets = [3,5,4]

输出： 1

解释：

fruits[0] = 4 放入 baskets[1] = 5。

fruits[1] = 2 放入 baskets[0] = 3。

fruits[2] = 5 无法放入 baskets[2] = 4。

由于有一种水果未放置，我们返回 1。



C++版

```cpp
class SegmentTree {
    vector<int> mx;

    void maintain(int o) {
        mx[o] = max(mx[o * 2], mx[o * 2 + 1]);
    }

    // 初始化线段树
    void build(const vector<int>& a, int o, int l, int r) {
        if (l == r) {
            mx[o] = a[l];
            return;
        }
        int m = (l + r) / 2;
        build(a, o * 2, l, m);
        build(a, o * 2 + 1, m + 1, r);
        maintain(o);
    }

public:
    SegmentTree(const vector<int>& a) {
        size_t n = a.size();
        mx.resize(2 << bit_width(n - 1));
        build(a, 1, 0, n - 1);
    }

    // 找区间内的第一个 >= x 的数，并更新为 -1，返回这个数的下标（没有则返回 -1）
    int findFirstAndUpdate(int o, int l, int r, int x) {
        if (mx[o] < x) { // 区间没有 >= x 的数
            return -1;
        }
        if (l == r) {
            mx[o] = -1; // 更新为 -1，表示不能放水果
            return l;
        }
        int m = (l + r) / 2;
        int i = findFirstAndUpdate(o * 2, l, m, x); // 先递归左子树
        if (i < 0) { // 左子树没找到
            i = findFirstAndUpdate(o * 2 + 1, m + 1, r, x); // 再递归右子树
        }
        maintain(o);
        return i;
    }
};

class Solution {
public:
    int numOfUnplacedFruits(vector<int>& fruits, vector<int>& baskets) {
        SegmentTree t(baskets);
        int n = baskets.size(), ans = 0;
        for (int x : fruits) {
            if (t.findFirstAndUpdate(1, 0, n - 1, x) < 0) {
                ans++;
            }
        }
        return ans;
    }
};
```



### 树状数组
#### 动机
##### 询问
给你一个数组，如何快速地计算任意一段连续子数组的元素和？

对于一个子数组来说，如果遍历子数组的每个数，把它们加起来，时间复杂度是 O(n)，太慢了。

下标从 left 到 right 的子数组元素和，可以看成是下标从 1 到 right 的子数组元素和，减去下标从 1 到 left−1 的子数组元素和。例如数组 [3,1,4,1,5,9]，子数组 [4,1,5] 的元素和，等于 [3,1,4,1,5] 的元素和，减去 [3,1] 的元素和。

按照这个方法，算出每个前缀 [1,i]（表示下标从 1 到 i 的连续子数组）的元素和，就可以 O(1) 地计算**任意**连续子数组的元素和了。

##### 更新
但是，如果还可以**修改**数组中的元素呢？

比如我把下标为 1 的元素修改了，由于所有前缀都包含下标 1，那么就需要更新所有前缀的元素和，更新操作就需要 O(n) 的时间，这太慢了。

能不能把前缀 [1,i] 拆分成若干段连续子数组呢？

如果拆分得太细，比如拆分成 [1,1],[2,2],[3,3],⋯，虽然更新是 O(1) 的，但计算子数组元素和还是得遍历累加，时间复杂度是 O(n)，太慢了。

##### 平衡
上面的做法，要么询问是 O(1) 更新是 O(n)，要么询问是 O(n) 更新是 O(1)，时间差距悬殊。

如何「平衡」询问和更新的时间复杂度？

关键在于如何拆分子数组（区间）。

能否把任意前缀拆分成若干个**关键区间**，使得更新操作也只会更新若干个关键区间？

这样回答询问时，只需要遍历并累加若干个关键区间的元素和。更新元素时，也只需要遍历并更新若干个关键区间的元素和。



#### 如何拆分？
启示：如果把一个正整数 i 拆分成若干个不同的 2 的幂（从大到小），那么只会拆分出 O(logi) 个数。前缀能否也这样拆分呢？

举个例子，13=8+4+1，那么前缀 [1,13] 可以拆分成三个长度分别为 8,4,1 的关键区间：[1,8],[9,12],[13,13]。

按照这个规则，来看看从 [1,1] 到 [1,8] 是如何拆分的：

[1,1]=[1,1][1,2]=[1,2][1,3]=[1,2]+[3,3][1,4]=[1,4][1,5]=[1,4]+[5,5][1,6]=[1,4]+[5,6][1,7]=[1,4]+[5,6]+[7,7][1,8]=[1,8](1=1)(2=2)(3=2+1)(4=4)(5=4+1)(6=4+2)(7=4+2+1)(8=8)

数一数，按照这种拆分方式，总共有多少个不同的关键区间？（提示：把注意力放在区间的右端点上。）

有 8 个：[1,1],[1,2],[3,3],[1,4],[5,5],[5,6],[7,7],[1,8]。

一般地：

+ 如果 i 是 2 的幂，那么 [1,i] 无需拆分。
+ 如果 i 不是 2 的幂，那么先拆分出一个最小的 2 的幂，记作 lowbit(i)（例如 6 拆分出 2），得到长为 lowbit(i) 的关键区间 [i−lowbit(i)+1,i]，问题转换成剩下的 [1,i−lowbit(i)] 如何拆分，**这是一个规模更小的子问题**。

总共有 n 个不同的关键区间。

**证明**：按顺序拆分前缀 [1,1],[1,2],[1,3],…,[1,n]，每次只会恰好拆出一个**新的**关键区间 [i−lowbit(i)+1,i]（注意 [1,i−lowbit(i)] 之前拆分过了，不会产生新的关键区间），所以一共有 n 个不同的关键区间。

##### 算法
由于关键区间的右端点互不相同，我们可以把右端点为 i 的关键区间的元素和保存在 tree[i] 中。

按照如下方法计算前缀 [1,i] 的元素和：

1. 初始化元素和 s=0。
2. 每次循环，把 tree[i] 加到 s 中，对应关键区间 [i−lowbit(i)+1,i] 的元素和。
3. 然后更新 i 为 i−lowbit(i)，表示接下来要拆分 [1,i−lowbit(i)]，获取其中关键区间的元素和。
4. 循环直到 i=0 为止。
5. 返回 s。

由于正整数 i 的二进制长度是 ⌊log2i⌋+1，所以任意前缀至多拆分出 O(logn) 个关键区间，所以上述算法的时间复杂度为 O(logn)。

关于 lowbit(i) 的计算方法，请看 [从集合论到位运算，常见位运算技巧分类总结！](https://leetcode.cn/circle/discuss/CaOJ45/)

要计算 `sumRange(left, right)`，可以分别计算 [1,right+1] 的元素和（改成下标从 1 开始），以及 [1,left] 的元素和，两者相减即为答案。

接下来，看看更新一个数组元素时，会影响到哪些关键区间。

三、如何更新？

更新操作证明比较复杂，推荐结合 [视频](/link/?target=https%3A%2F%2Fwww.bilibili.com%2Fvideo%2FBV14r421W7oR%3Ft%3D26m07s) 理解。

假设下标 x 发生了更新，那么所有包含 x 的关键区间都会被更新。

例如下标 5 更新了，那么关键区间 [5,5],[5,6],[1,8],[1,16] 都需要更新，这三个关键区间的右端点依次为 5,6,8,16。

如果在 5-6,6-8,8-16 之间连边（其它位置也同理），我们可以得到一个什么样的结构？

如下图，这些关键区间可以形成如下树形结构（区间元素和保存在区间右端点处）。

![null](https://cdn.nlark.com/yuque/0/2025/png/179650/1755151852124-e4410df2-9ba8-49dd-aa0f-81e43760f7a3.png)

注意到：

5+lowbit(5)=5+1=66+lowbit(6)=6+2=88+lowbit(8)=8+8=16

**猜想**：如果 x 是一个被更新的关键区间的右端点，那么**下一个**被更新的关键区间的右端点为 x+lowbit(x)。

我们需要证明两点：

1. 右端点为 x 的关键区间，被右端点为 x+lowbit(x) 的关键区间**包含**。
2. 右端点在 [x+1,x+lowbit(x)−1] 内的关键区间，与右端点为 x 的关键区间**没有任何交集**。

##### 1 的证明
设 y=x+lowbit(x)，由于 y>x，我们只需要证明这两个关键区间的**左端点**满足

y−lowbit(y)+1≤x−lowbit(x)+1

即

y−lowbit(y)≤x−lowbit(x)

就能证明包含关系。

设 lowbit(x)=2k，那么 x=m⋅2k+1+2k，这里 m 是一个非负整数。

所以不等式右边为

x−lowbit(x)=m⋅2k+1

由于 y=x+lowbit(x)=(m+1)⋅2k+1，得到 lowbit(y)≥2k+1，

所以不等式左边为

y−lowbit(y)≤m⋅2k+1

综上所述

y−lowbit(y)≤m⋅2k+1=x−lowbit(x)

这说明包含关系是成立的。

##### 2 的证明
设 y=x+b，其中 1≤b<2k（k 的定义同上）。

右端点为 y 的关键区间，左端点为 y−lowbit(y)+1。我们只需要证明

y−lowbit(y)+1>x

就能证明右端点为 y 的关键区间，一定在右端点为 x 的关键区间的右侧，它们是没有任何交集的。

由于 y=x+b=m⋅2k+1+2k+b 且 b<2k，所以

lowbit(y)=lowbit(b)

又由于

b−lowbit(b)≥0

所以

= = ≥ > y−lowbit(y)+1(x+b)−lowbit(b)+1x+(b−lowbit(b))+1x+1x

证毕。

以上两点成立，就可以保证 x+lowbit(x) 是「下一个」被更新的关键区间的右端点了。

由于任意相邻被更新的关键区间之间，没有其余关键区间包含 x，所以我们可以找到**所有**包含 x 的关键区间，具体做法如下。

##### 更新算法
对于 `update(index, val)`，算法如下：

1. 设 delta=val−nums[index]，相当于把 index 的元素增加了这么多。然后把 nums[index] 更新成 val。
2. 初始化 i=index+1（注意下标从 1 开始），这是第一个被更新的关键区间的右端点。
3. 不断循环直到 i>n，这里 n 是 nums 的长度。
4. 每次循环，把 tree[i] 增加 delta。
5. 然后更新 i 为 i+lowbit(i)，即下一个被更新的关键区间的右端点。



#### 复杂度分析
在「1 的证明」中我们得到

lowbit(y)≥2k+1>2k=lowbit(x)

这说明随着 i 的不断更新，lowbit(i)=2k 是不断增大的，那么 k 也在不断增大。由于至多有 O(logn) 个不同的 k，所以上述算法的时间复杂度为 O(logn)。

至此，通过巧妙地拆分前缀，我们做到了询问和更新都是 O(logn) 的时间！



如何初始化？

O(nlogn) 写法

最后剩下的问题是，如何根据 nums 初始化，也就是把每个关键区间的元素和 tree[i] 算出来。

最简单的做法是，把 tree[i] 初始化成 0，然后对每个 nums[i]，调用一次 `update(i, nums[i])`。



#### 树状数组 VS 线段树
树状数组（Fenwick Tree）和线段树（Segment Tree）都是处理**区间查询**和**单点 / 区间更新**的高效数据结构，常用于解决数组的前缀和、区间最值、区间和等问题。两者在功能上有重叠，但设计思路和适用场景存在显著差异。

核心共性

1. **问题领域**：均用于处理一维数组的**区间查询**（如区间和、区间最值）和**动态更新**（单点修改或区间修改）操作。
2. **效率目标**：均通过**分治思想**和**预处理**，将原始的`O(n)`查询 / 更新复杂度优化到`O(log n)`。
3. **适用场景**：适用于需要频繁进行 “查询 + 更新” 的场景（如统计、调度、动态规划等）。

核心差异

| **特性** | **树状数组（Fenwick Tree）** | **线段树（Segment Tree）** |
| --- | --- | --- |
| **结构** | 采用**紧凑的数组结构**，通过二进制低位标记父子关系（非严格树形）。 | 采用**完全二叉树结构**，每个节点代表一个区间，左右子节点平分父节点区间。 |
| **功能范围** | 功能有限，仅支持**前缀和查询**（可扩展到区间和）和**单点更新**，不直接支持区间最值、区间修改等复杂操作。 | 功能全面，支持**任意区间查询**（和、最值、计数等）和**任意区间更新**（加减、覆盖等）。 |
| **实现复杂度** | 代码简洁，核心操作仅需 2-3 个函数（`update`、`query`），易于实现和调试。 | 代码较长，需处理左右子树划分、懒标记（Lazy Propagation）等，实现复杂度高。 |
| **空间复杂度** | 更节省空间，仅需`O(n)`（与原数组大小相同）。 | 需`O(4n)`空间（为保证完全二叉树结构，通常开 4 倍原数组大小）。 |
| **适用场景** | 适合简单场景：前缀和、单点更新、逆序对统计等。 | 适合复杂场景：区间最值、区间加减、区间覆盖、多维问题等。 |


功能对比示例

1. **区间和查询**：
    - 树状数组：通过前缀和相减实现（`sum(l, r) = query(r) - query(l-1)`），仅支持单点更新。
    - 线段树：直接查询任意区间和，支持单点更新和区间批量更新（需懒标记）。
2. **区间最值查询**：
    - 树状数组：**不直接支持**（需特殊改造，效率较低）。
    - 线段树：天然支持，通过比较左右子树最值实现。
3. **更新操作**：
    - 树状数组：仅支持**单点更新**（如`add(i, val)`）。
    - 线段树：支持**区间更新**（如`add(l, r, val)`），通过懒标记优化效率。

本质关系

+ **树状数组是线段树的简化版**：树状数组通过限制功能（仅处理前缀相关操作），实现了更紧凑的结构和更简单的代码，是线段树在特定场景下的高效替代方案。
+ **线段树是树状数组的泛化版**：线段树通过更灵活的区间划分和懒标记技术，支持更复杂的操作，适用范围更广，但代价是更高的实现复杂度和空间消耗。

选择建议

+ 若问题仅涉及**前缀和 / 区间和**和**单点更新**（如逆序对、频率统计），优先用**树状数组**（代码简单、效率高）。
+ 若问题涉及**区间最值**、**区间批量更新**或**复杂查询**（如区间众数、区间 GCD），必须用**线段树**。



### 参考链接
[https://blog.csdn.net/u010009169/article/details/123537025](https://blog.csdn.net/u010009169/article/details/123537025)

[https://zhuanlan.zhihu.com/p/308150123](https://zhuanlan.zhihu.com/p/308150123)

[https://blog.csdn.net/Lyt15829797751/article/details/81054920](https://blog.csdn.net/Lyt15829797751/article/details/81054920)

[https://blog.csdn.net/m0_52383454/article/details/126393163](https://blog.csdn.net/m0_52383454/article/details/126393163)

[https://blog.csdn.net/ThinkWon/article/details/102571883](https://blog.csdn.net/ThinkWon/article/details/102571883)
